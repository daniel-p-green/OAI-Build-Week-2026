# WorkshopLM Goal

Last updated: 2026-07-16 16:00 CT

## Status

### 2026-07-16 live-product truth — supersedes older provider-risk prose below

- The judge-facing demo film now renders through pinned local HyperFrames 0.7.60 instead of the former FFmpeg `zoompan` compositor that could produce visible pixel-step jitter. The composition stages and hashes the active `DESIGN-v1.md` / tokens and approved `FRAME-v1.md` / executable frame, uses their exact paper, ink, Accent, typography, outcome, and restrained editorial-motion rules, and records those identities in the film manifest. The same fail-closed DESIGN + FRAME provenance now ships beside every product Video render. New public and UI language says `Slides`; `deck` remains only as a private backward-compatible domain identifier or immutable historical evidence. Slides are the lead professional deliverable, not the product limit: visuals, Audio Overview, Infographic, Storyboard, and Video remain first-class Outputs.
- The complete paid media seam has run successfully from the authorized sample voice brainstorm: `gpt-5.6-terra` grounded Map, six coherent `gpt-image-2` visuals, five current `gpt-4o-mini-tts` Cedar clips, and a 25.00-second local HyperFrames MP4 with H.264 video and AAC audio. The current local Video is immutable Version 4, renders a calm editorial GSAP timeline locally through HyperFrames, and preserves its exact Storyboard, Style, image, narration, claim, source, build-trace, and file-hash edges.
- `How this was built` is now a judge-ready product proof instead of a technical appendix. The contextual Original brainstorm reveal makes it the first action after the before/after transformation; the generated HTML follows Capture → Shape → Deliver, uses the active Style, shows both sign-offs and every current connected Output with exact hashes and source-linked claims, and links to immutable versioned JSON. The milestone timeline is chronological even when append-only corrections arrive late. Desktop and mobile proof are under `artifacts/ui-review/build-trace-2026-07-16/`, and all 31 production-browser cases now reset through an isolated seeded-fixture contract instead of inheriting state from earlier tests.
- The sanitized judge Workshop now replays the exact provider-generated 35.7-second `gpt-4o-mini-tts` Cedar Audio Overview with no credential or paid call. Seeding fails closed unless the current script, sections, claim IDs, exact Source edges, model, voice, request ID, byte count, and SHA-256 all match the tracked provider evidence. The focused Output keeps playback visible at desktop and mobile widths, names the AI-generated voice, and preserves the three editable source-linked sections; accepted screenshots are `artifacts/ui-review/judge-audio-overview-*-2026-07-16.png`.
- The final film's meta-reveal now proves its own trace instead of merely narrating it. The compositor reads the final submission manifest and `BUILD-TRACE.json` at render time and places the actual connected Output, source-linked claim, two-sign-off, and hashed-asset counts inside the existing final frame. Missing trace data now stops final rendering, and `build-record` is an explicit required film moment. The inspected preview shows `7 Outputs · 6 source-linked claims · 2 sign-offs · 25 hashed assets` from the acceptance package; final numbers will be derived anew from the founder package rather than copied.
- A complete clean 2:20 sample editorial cut now exercises the final visual language before founder evidence exists. `pnpm demo:film:sample` removes fixture banners, keeps the verified ten-clip OpenAI Cedar narration, uses the authorized sample transcript and acceptance Output set, and generates the traced final reveal inside the actual H.264/AAC movie. The existing privacy-safe Codex in-app-browser footage now carries one restrained `CODEX → WORKSHOPLM` transition cue, making the plugin doorway unmistakable without fabricating host UI. The cut is explicitly labeled `AUTHORIZED SAMPLE · EDITORIAL CUT`, retains both final-evidence shots as blocked in its manifest, and cannot be mistaken for the public submission. `pnpm demo:film:verify-sample` rehashes the transcript, acceptance manifest, `BUILD-TRACE.json`, movie, contact sheet, and all ten review frames and requires the doorway cue; the accepted video is 140.021 seconds with SHA-256 `5ac26ed6505b1402d83b33499c78cca36fa2c58f8663116fa26be6a30427d1d8`. Objective QC found no black, frozen-over-3.5-second, or silent-over-2.5-second interval; integrated loudness remains -22.9 LUFS with -2.9 dB true peak.
- The clean sample movie is now the canonical pre-founder review artifact across the public README, demo handoff, claim ledger, GOAL evidence links, and submission-packet verifier. The badge-heavy rough cut remains a diagnostic artifact only. Packet verification hashes the clean movie, its ten review frames, acceptance manifest, and build trace; requires the sample disclosure, two blocked states, generated meta-reveal, and Codex doorway cue; and reports the rough cut separately. The current public evidence floor now says 13 packages, 120 worker tests, 30 web tests, and 31 production-browser scenarios instead of stale lower counts.
- Two nine-request Sol/Terra/Luna benchmark passes completed. All deterministic checks passed; the final comparable graph pass measured Terra at 2.815 seconds and 379 output tokens versus Sol at 6.815 seconds and 424 output tokens, so Terra is now the grounded-Map default. The live Map records provider request ID `req_11ee10453c1e45cd8067e578171f82d6`.
- A separate one-request external-use proof now records AI Collective evidence becoming a six-node Terra Map in 5.957927 seconds and a first editable Presentation in 5.983927 seconds from the normalized Source timestamp. Inspection rejected that first draft, exposed source-locator metadata leakage and one clipped transformation, and drove a product-level repair. Accepted Presentation Version 3 regenerated locally without another provider request and passed PowerPoint, PDF, extracted-text, desktop, and compact review under `artifacts/live-review/first-fifteen-external/`. This is controlled evidence, not an uncoached professional-own-material result.
- The evidence ledger now records 107 OpenAI provider HTTP operations across the benchmark, Map, image, Cedar narration, Responses, Realtime, editorial-film, external-use first-draft proof, and sanitized judge-image runs. This includes ephemeral Realtime credentials and WebRTC calls rather than treating only completed generations as requests. Exact dollar debit is not returned by these provider responses; observed request shape remains safely below the authorized $50 ceiling. The latest three requests selectively regenerated only the Cedar narration clips whose terminology changed from `deck` to `slides`; no image, text, Realtime, or video provider call was repeated.
- Visual inspection accepted all six GPT Image 2 panels without regeneration. The renderer now uses a clean editorial image/copy split, quiet AI-voice disclosure, and 0.92 Cedar volume instead of treating images as dark wallpaper. Evidence is under `artifacts/live-review/` and `.workshoplm/live-operator/`.
- The current editable Presentation Version 6 carries the approved GPT Image 2 hero and its exact `image-batch-v1` / `image-panel-1` Version 1 / SHA-256 dependency. Spoken transformation language now becomes a complete executive headline plus supporting thought instead of a clipped sentence, while explicit three-stage process language becomes an editable Capture → Shape → Deliver sequence rather than decorative numbering. Its PowerPoint, five-page 16:9 LibreOffice PDF, extracted source labels, individual slides, and accepted contact sheet are `artifacts/live-review/presentation-v6/`. Replacing the bound image still marks the Presentation `Needs update`; this repair used no provider request.
- Presentation process slides now carry a complete information hierarchy instead of three nearly empty cards. Capture, Shape, and Deliver each receive one neutral, non-claiming definition in both responsive HTML and editable PowerPoint; the PowerPoint round trip is visually accepted under `artifacts/live-review/presentation-sequence-detail/`. The wording clarifies stage meaning without inventing evidence beyond the approved Source.
- The current editable Infographic Version 4 now uses the same semantic content decisions without inheriting Presentation-only layout rules. The real authorized-sample output replaces its clipped opening claim and generic process card with a complete idea plus a visual Capture → Shape → Deliver sequence in responsive HTML and editable PowerPoint. Its archive, one-page LibreOffice PDF, extracted text, desktop/compact workbench proof, gallery, and rebuilt film evidence are accepted under `artifacts/live-review/infographic-v4/`; no provider request was added.
- A fresh send-it review inspected the actual provider-backed Slides, Infographic, six-image set, and HyperFrames Video composition together. The image set clears the professional-use bar for coherent slide visuals; the motion composition is restrained, legible, and visually consistent with the approved Style. The Slides and Infographic are clean, editable, source-traced product proofs, but the authorized sample Source is too thin to establish a board-ready content-depth claim. No new renderer, prompt, or UI defect was found to justify another local change or provider call. The decisive remaining test is the founder-derived Output set plus an intended-audience `Send` or concrete `Revise` judgment; do not manufacture unsupported depth from the sample transcript.
- The live UI now has one contextual workbench instead of a dashboard: Sources + Chat on the Map, a five-item Create summary, auto-collapsed context around Outputs and focused artifacts, and no persistent stage stack. `Voice brainstorm` replaces timestamp-heavy capture copy. Desktop and 390×844 Map, Outputs, and Storyboard screenshots are under `artifacts/ui-review/`.
- The second whiteboard mode is now a real product surface instead of worker-only state. `Create outputs` regenerates a deterministic 1600×900 hand-drawn SVG from the approved semantic Map, current Style tokens, evidence states, and exact graph edges. Sketch appears as one supporting Output—not another tab or editor—with SVG download, full-size review, claim-to-Source trace, graph/style version and file-hash provenance, calm `Needs update` behavior, and immutable prior versions that remain reopenable and downloadable after regeneration. Desktop, mobile, and history production-browser baselines verify the focused object, natural image dimensions, version-specific routes, top-of-object entry, and zero horizontal overflow. Workshop summaries count Sketch once as an Output family instead of inflating the count for every revision.
- Approved Storyboards now meet the same immutable-history bar as the finished Video. Editing a panel or binding a regenerated GPT Image 2 panel creates a new unapproved Storyboard version, archives the earlier version, and preserves whether it was approved plus every panel's exact image path and SHA-256. Earlier Storyboards reopen read-only in the existing focused sheet with their reviewed narration, duration, Sources, and version-specific image bytes; they cannot be mistaken for the current approval target. Replacement still stales narration and every prior Video while leaving the professional's approved history intact.
- Storyboard timing is now professionally editable instead of being a hidden generated value. The focused panel exposes one official `Seconds` field beside title and narration, validates a whole number from 1 to 120 in both UI and service, and routes a timing change through the same new-version, approval-revocation, narration-staleness, and Video-staleness contract. Desktop, compact, and mobile production-browser proof keeps timing visible without another mode or control panel.
- Selective image replacement now preserves professional revision history instead of overwriting the reviewed visual. Starting a replacement archives the generated image's exact prompt, evidence, path, SHA-256, and provider provenance, while the existing image remains visible until the replacement passes validation. The existing `Replace image` sheet can preview and open every exact version through a version-specific route; no tab or new navigation surface was added.
- The live provider-backed Outputs gallery now shows only the current version of each deliverable; older versions move into a focused `Version history` instead of duplicating full-size gallery cards. Visible source trails say `Voice brainstorm` or the professional Source title while exact provider locators remain preserved in hidden trace metadata. The real desktop and 390×844 gallery plus compact Presentation history were inspected in the Codex in-app browser and captured under `artifacts/ui-review/outputs-latest-only-*` and `presentation-version-history-compact-2026-07-16.png`.
- Focused professional Outputs now show the complete artifact before its trace. Presentation, Infographic, and Video use their real 16:9 frame inside one scroll surface; Sources and version history follow instead of shrinking or overlaying the artifact. The provider-backed Presentation cover now fits completely at 1200×800, 1024×768, and 760×900 with zero horizontal overflow. The browser contract verifies slide and title fit, and accepted screenshots are under `artifacts/ui-review/presentation-fit/`.
- Focused Output actions now match the professional's finishing task. Presentation and Infographic make the editable PowerPoint the single primary action; Video makes `Edit storyboard` primary. The duplicate header-level `Show source` is gone because the complete claim-by-claim source trail remains immediately below the artifact. Full-size preview, original-brainstorm reveal, immutable history, and exact source access remain available as secondary actions.
- The compact and mobile workbench no longer renders a persistent `Chat / Map / Brief / Outputs / Story` tab strip. One quiet `Views` control opens an official-component sheet with the five objects, short plain-language purposes, current state, and unavailable states. Sources and the current next action remain visible in the header, while the dominant object reclaims the full canvas.
- Source-backed work is no longer labeled `Verified` in the professional UI. The Map and claim inspector say `Sourced`, the Brief says `Sources behind this brief` and reports sourced claims, and Chat says answers stay linked to Sources. This preserves the underlying evidence states while avoiding the false implication that attaching a Source proves a statement true. The same pass fixed a subtle sheet interaction: focus and the focused Output's exact scroll position now return together, so closing Original brainstorm or Source detail cannot jump the work under the header.
- Voice provenance now stays professional across every visible trace surface. The sanitized fixture, local fallback, and founder import appear as `Voice brainstorm` or `Founder brainstorm`; the before/after reveal says `Voice transcript`; and Source evidence shows a display-safe position such as `Voice brainstorm · chunk 04` instead of task/provider mechanics. The exact raw locator remains unchanged in persisted provenance. Closing a sheet restores focus and keeps the focused object title visible below the header across desktop, compact, and mobile; browser scroll anchoring can no longer pull the artifact underneath it.
- The public judge setup now passes from a fresh clone of commit `4cd938d`: frozen install, checks across all thirteen packages, recorded acceptance, a second local HyperFrames render, thumbnail generation, and the live Workshop/API. The audit repaired four false-green risks in the documented path: render and thumbnail scripts now use the acceptance data root, approvals are not destructively replayed, HyperFrames 0.7.60 is pinned and uses its current `check` command, and a failed worker result exits nonzero. The public README now reflects Terra/Image 2/Cedar/Realtime proof without conflating the deterministic fixture, authorized sample run, or still-open founder package.
- The optional judge path is now one exact command after a frozen install: `pnpm judge:start` rebuilds the sanitized acceptance Workshop and serves that verified data root. Its six exact GPT Image 2 files are checked in with request provenance and SHA-256 manifests, copied into the isolated data root, rebound to the Presentation and five Storyboard panels, and rendered locally without credentials or paid replay calls. A live port-3113 smoke run returned HTTP 200 for the page, Workshop API, and image artifact route; the API exposed six generated `gpt-image-2` panels, the hero dependency, five image-version Storyboard edges, and rendered Video. The README and Devpost draft no longer send judges to the unrelated default workspace through `pnpm dev`, and the packet verifier rejects that regression.
- Clean-clone milestone `3d7221b` independently passed the public path from a fresh shallow clone: frozen install, all thirteen-package checks, recorded seam, second local HyperFrames render, deterministic thumbnail, submission-packet verification, and the one-command live workbench. Next selected port 3001 because 3000 was occupied; `/` and `/api/workshop` both returned HTTP 200 with the expected approved Brief, approved Storyboard, rendered Video, one Source, Presentation, and Infographic. Later verified milestones preserve that proof while the public submission gates remain open.
- The main `Add source` path now preserves pasted professional notes as `private`, matching onboarding, local PDFs, and Realtime transcripts instead of silently labeling them demo-safe `sanitized` material. A production-browser regression captures the actual ingestion request and enforces that boundary. The same pass replaces visible grounding/provider jargon with direct promises: claims are sourced, Map ideas are organized, voice answers use only selected Sources, and an unverifiable transcript asks for a retry without exposing provider mechanics.
- Public submission packaging now fails closed if any active Source remains `private`; it will not copy private Source titles, locators, claims, transcripts, or build-trace excerpts into the shareable Output set. Founder recording imports remain private by default, their raw staged film files are ignored by Git, and only the explicit `--share-founder-source` consent flag permits that material to enter the local public-package path. The flag does not upload or publish anything.
- The finished Video now exposes `Edit storyboard` beside its source, original-brainstorm, and playback actions. A changed panel says exactly what saving will do, `Save changes` revokes Storyboard approval, the existing Video becomes `Needs update`, and `Approve storyboard` becomes the one dominant next action. Video staleness no longer incorrectly offers to regenerate unrelated Outputs. The complete revision loop is locked by the production-browser suite at desktop, compact, and mobile widths.
- Image replacement is now a professional revision loop rather than a blind retry. The focused official-component sheet keeps the current grounded image visible, accepts one plain-language change of at most 400 characters, preserves the approved idea, Style, evidence, and base prompt, and sends that instruction only to the selected GPT Image 2 panel. Pending work keeps the existing image on screen, shows the requested change, revokes Storyboard approval, and requires review before Video. Desktop and 390×844 snapshots are locked in the production-browser suite.
- Voice provenance now has an explicit disclosure boundary: Sources navigation uses `Voice brainstorm` and `Voice` instead of provider model, timestamp, or fallback mechanics; the semantic Map says `Voice stays available`; the Source evidence sheet still reveals the exact unchanged locator needed to defend the claim. A dedicated 390×844 regression proves provider transport language stays out of the primary Sources surface without weakening traceability.
- The canonical fixture recording now follows the current capture-first onboarding and native workbench instead of the retired pre-onboarding selectors. Its 58.0-second, 12-beat run visibly proves the Sources rail, exact Source reveal, Map edit, Brief approval, exact Style choice, real provider-backed imagery in the Presentation and Storyboard, focused source-traced Sketch, Storyboard edit and approval, local image-bound render, and connected Output-set original-brainstorm reveal. The 2:20 editorial rough cut was rebuilt from this footage with the already verified ten-clip OpenAI Cedar narration; the default build now reuses that verified manifest instead of silently downgrading to a local guide voice.
- The 2:20 judge-film edit now separates spoken narration from ten deliberate on-screen proof captions. A full-frame audit found the HyperFrames/OpenAI render explanation clipped behind an ellipsis; the plan now requires a complete caption of at most 80 characters for every shot, the compositor renders that field in both rough and final styles, and the rebuilt ten-frame sequence has no truncated proof statement. Founder capture and the final meta-reveal remain the only two blocked shots.
- The judge-film compositor now owns the representative hold frame for every short captured beat instead of blindly freezing the last frame after the action. Visual audit had found evidence/edit narration holding on the already-approved Brief, Brief-approval narration holding on the next Style sheet, and Style narration holding on the later Outputs page. Bounded per-shot end holdbacks now keep the proof aligned: editable claim inspector, approved Brief, applied Style, and approved Storyboard each remain visible under their corresponding narration.
- Objective film QC rejected the long static holds that remained after editorial retiming. Every rough-cut shot now receives a restrained push-and-drift across its proof surface; the longest low-motion interval fell from repeated 12–18 second holds to one 3.03-second tail on the honestly blocked founder-capture placeholder. The current 2:20 H.264/AAC cut has no detected black interval, preserves all ten legible proof captions, and retains the verified Cedar mix at -22.9 LUFS integrated loudness, 4.4 LU range, and -2.9 dB true peak. Human taste review and the real founder replacement remain open.
- The submission thumbnail is no longer an arbitrary frame extracted one second into the local Video. `pnpm demo:thumbnail` now renders a deterministic 1280×720 WorkshopLM composition from the locked film-plan promise and the canonical provider-backed Outputs screen, binds the rendered Video and product proof by SHA-256, and writes both a data-root submission asset and a checked-in preview manifest. Native and 640×360 review accepted the headline, product identity, and real finished-work proof; packet verification rejects changed copy, dimensions, hashes, product evidence, or third-party marks.
- The same send-it treatment now reaches the actual traced submission Output set instead of stopping at a standalone preview. `buildSubmissionOutputSet` creates `thumbnail-opening.png` from the current Workshop's locked Style, active Source/claim counts, and first current generated image when available; `thumbnail-process.png` and `thumbnail-result.png` remain timestamped Video proof frames. The 18-asset acceptance package rebuilt and verified untampered with the styled cover, and the production path will fail if a recorded generated image no longer matches its stored hash.
- The traced package's writing now clears the same product bar as its media. `DEVPOST.md` is a 649-word state-aware judge narrative built around the professional handoff gap, the complete product path, the two sign-offs, claim receipts, reusable Style, editability, the meta-demo, OpenAI/Codex roles, the one-command judge path, and dynamically computed provider evidence and limitations. `README-NARRATIVE.md` is a 290-word package map with the exact six-step flow, included artifacts, reproduction command, state versions, and evidence boundary. The acceptance package regenerates and verifies these files from its current Workshop state rather than copying the hand-authored submission draft or shipping generic scaffold copy.
- The clean final-film preview now fails clean instead of silently reporting a stale meta-reveal image. It removes every prior preview before rebuilding, uses a real provider-Video still instead of a loading frame, and labels the inevitably bounded founder text honestly as a `Verbatim excerpt` rather than the full unedited transcript. A freshly built and verified sanitized submission package produced the current inspected preview; twenty retired untracked gallery screens were preserved outside the repository after the strict packet gate rejected them.
- Film evidence and Devpost evidence now have one enforced boundary: the final founder-derived Output set unlocks the meta-reveal shot, while `/feedback` remains a separate submission-form requirement. The rough-cut handoff no longer incorrectly blocks shot ten on a Session ID, and the manifest-backed packet verifier now fails if that contradiction returns.
- The submission packet now matches the live proof floor instead of the pre-spend state: Terra Map, six GPT Image 2 panels, five product Cedar clips, ten editorial Cedar clips, and grounded Realtime are promoted from gated to verified with their exact evidence boundaries. The redundant stale Devpost draft was removed. `pnpm submission:packet:verify` now fails if those claims regress, the provider/film manifests drift, the 2:20 rough cut loses Cedar, the eight-ready/two-blocked film state changes unexpectedly, or unresolved brackets expand beyond the four founder/final-package slots.
- The canonical shareable UI gallery now contains sixteen current-workbench screens instead of the discarded tabbed MVP. Its manifest binds every screenshot by source path, dimensions, and SHA-256; its contact sheet and ZIP are rebuilt by `pnpm ui:gallery:build`. The demo-film overlay preview now consumes those current filenames, and submission verification rejects retired UI evidence or a changed gallery hash. Screen 08 is the inspected provider-backed Outputs gallery; the remaining screens are explicitly labeled sanitized fixture evidence.
- Provider-backed Realtime is now live-proven over WebRTC with `gpt-realtime-2.1` and `gpt-realtime-whisper`: a spoken question invoked one grounded search plus two exact fetches, produced a source-bound spoken answer, and persisted transcript, assistant text, provider item/event IDs, and tool provenance. A second live session recorded a real provider cancellation/interruption edge before the user redirected the answer. Evidence and clean product screenshots are `artifacts/live-review/realtime-grounded-conversation.*` and `realtime-interruption.*`.
- A third isolated Realtime run completed the full write-control loop. The provider requested `Approve Brief` once with the exact current Workshop and Map version; WorkshopLM rejected mutation without explicit intent, showed the visible confirmation, replayed the same provider call ID after confirmation, persisted `briefApproved: false → true`, and returned the success to the active voice session for a spoken continuation. Evidence is `artifacts/live-review/realtime-confirmed-write.*`.
- The center-column text Conversation is now provider-proven through Responses SSE with `gpt-5.6-terra`: one user question invoked grounded `search` and exact `fetch`, persisted three claim-to-source edges plus the continuation response ID, and rendered the final source-bound answer. The live run also proved that three provider requests can be consumed by retrieval before the final answer, so the bounded default is now four. Evidence is `artifacts/live-review/responses-grounded-conversation.*`.
- A standalone source-grounded Audio Overview is provider-proven with `gpt-4o-mini-tts` and Cedar. The persisted and downloaded WAV hashes match; the 35.7-second, 24 kHz mono file was audibly inspected by a local Whisper round trip that preserved the intended claims, exact-source-locator point, and professional briefing structure. Evidence is `artifacts/live-review/audio-overview.*`.
- The inspected live provider run is now packaged as fail-closed film evidence: sanitized Terra/Image 2/Cedar request provenance, six verified image hashes, five product-Video narration hashes, one H.264/AAC provider-narrated Video, and a six-second live Outputs-gallery shot. Ten additional bounded Cedar clips now narrate the complete editorial film. Natural speech review tightened the same ten-beat story from 2:42 to 2:20 instead of slowing the voice into dead holds. Its truth gate has eight ready shots and only two blocked shots: founder capture and the final meta-reveal.
- The founder handoff is now executable rather than manual: `pnpm demo:founder -- --founder-recording <video> --founder-transcript <text>` validates a real audio-plus-video recording, validates transcript content, copies both into a separate `.workshoplm/final-operator` evidence root and the film-input directory, binds both copies by SHA-256 manifests, imports the transcript as a private grounded voice Source, and prepares the complete no-spend production plan. It explicitly records `providerRealtimeEvidence: false`; a file import never satisfies the separate provider-verified Realtime evidence gate. The command, film verifier, and custom-root paid/retry instructions were integration-tested with a temporary H.264/AAC MOV and are ready for the real files without risking the existing paid sample run.
- The final operator now owns the complete post-recording bridge. Its explicit ceiling is thirteen provider requests: one Terra Map, six GPT Image 2 panels, five Cedar Storyboard clips, and one grounded Cedar Audio Overview. After the approved HyperFrames render, it automatically builds and verifies the traced submission Output set under `.workshoplm/final-operator`; the film truth gate now reads that exact manifest rather than the deterministic acceptance fixture. A file-import-only run will remain honestly `partial` on the Realtime criterion; a provider-verified microphone turn in the same final Workshop permits `ready` when every other provider artifact passes.
- The final edit is now a deterministic one-command handoff rather than an undocumented manual assembly. `pnpm demo:film:final` refuses to start until the founder recording/transcript and cryptographically verified ready submission package exist, then replaces the capture beat, generates the raw-transcript/submission reveal, applies the inspected quiet editorial treatment, assembles the Cedar-narrated 2:20 H.264/AAC film, and promotes the plan to final only after the final verifier passes. A failed build restores the draft plan and removes the false final. The ten-frame overlay and meta-reveal previews are visually inspected; no public MP4 is claimed yet.
- The Realtime session schema now matches current provider behavior: `gpt-realtime-2.1` rejects `audio.input.transcription.prompt`, so the unsupported field was removed and locked with a regression test. Overlapping ASR completions no longer duplicate a spoken phrase; both `response.cancelled` and a cancelled `response.done` become durable interruption provenance. Recovered read-tool retries stay out of the calm activity summary while unrecovered failures remain visible.
- Excalidraw automatic line wrapping no longer masquerades as a user edit. A verified recovery restored the paid fixture to graph revision 24 without regeneration. Map movement and resizing now preserve approved work; only a content change invalidates the Brief and downstream artifacts. The live Map was arranged into a collision-free narrative grid with connectors behind its cards.
- Still open and not claimed: the founder's physical-microphone recording in the Codex in-app browser, replacement of the authorized sample with that founder Source, external professional review, final public submission film/links, and `/feedback`. Automated in-app-browser proof stopped honestly at the browser's microphone-permission prompt; controlled Chrome proved the complete provider/WebRTC read, interruption, confirmed-write, and spoken-continuation path without altering the live operator fixture.

**Active phase:** Stable workbench, professional send-it quality, and live demo proof
**Implementation:** Fully authorized for the complete locked WorkshopLM version in this file. Product discovery is closed; execute, verify, and ship.
**First-15-minute UX evidence:** Fresh post-Brief Style setup now defaults to the website path, keeps manual entry as the fallback, and shows the already-selected Workshop outcome as context instead of asking the professional to choose it again. Saved Company Styles inherit the current Workshop outcome when applied rather than silently restoring the outcome stored on the brand revision. The complete initial decision fits at 1280×720 and 390×844 with no horizontal overflow. The complete 30-case production-browser suite now exercises the capture-first onboarding order, website/manual Style paths, outcome-preserving Style reuse, responsive Map fit, connected Output-set reveal, focused Sketch, source-scope confirmation, image-specific recovery, voice language, and the current Storyboard/Video path with a clean no-update screenshot run.
**Next increment:** Record the founder brainstorm in the Codex in-app browser if microphone permission is available; otherwise provide the contemporaneous recording plus transcript to the verified `pnpm demo:founder` handoff. The latter remains an honestly labeled private voice import, not provider Realtime proof. Keep it private while reviewing the new Workshop; add `--share-founder-source` only when intentionally authorizing the raw founder material for the public meta-demo package. Use the resulting isolated `.workshoplm/final-operator` Workshop to replace the authorized sample Source and selectively regenerate only the final Source-dependent work. Once its submission manifest verifies as `ready`, run `pnpm demo:film:final`; the local HyperFrames compositor now owns the under-three-minute assembly and refuses false finality. The paid sample Map, images, Cedar narration, standalone Audio Overview, grounded Responses text, Realtime read/interruption/confirmed-write, and HyperFrames seams remain preserved. Then send the current dogfood Slides packet for one intended-audience `Send` or blocking `Revise` response.
**Stable-workbench evidence:** Phase 2 is implemented and verified: Sources remain visible on the left, durable grounded Conversation or the current work object stays centered, Production remains visible on the right, and mobile retains direct access to Chat, Map, Brief, Outputs, and Storyboard. Both desktop rails collapse to quiet 48px restore handles, expanding the 1280px canvas from 808px to 1184px without changing Workshop state; compact widths retain their existing Source sheet and object switcher. Text questions remain queries rather than silently becoming Sources; answers persist retrieval edges and open the exact cited excerpt. Voice capture shares the same surface and explicitly becomes a private Source. Changing selected Sources now pauses before mutation, selects the affected Source and keeps its excerpt visible, names the exact current work that will need an update, and confirms that Style stays the same. Only `Update sources` applies the new scope; the browser proof moved two Sources to one, restored the six-idea Map, kept Style `Ready`, and marked Brief, Presentation, Infographic, Image set, Storyboard, and Video `Needs update`. Production now retains calm failed, cancelled, partial, replacement, and retry states in the same rail: Video automatically stops after two attempts, a queued render can be cancelled, Presentation/Infographic failures persist until a successful retry, and a failed image routes to one-panel replacement without permitting premature Storyboard approval. One strict registry now defines the identical text/voice tool names, JSON schemas, source-scope effects, approval intent, and local-only MCP annotations. A shared local server executor validates those schemas, enforces active Source scope and exact Map/Storyboard versions, requires explicit user intent for writes, applies visible Workshop mutations, persists successful and failed calls/results with provider IDs, and safely replays provider retries. The browser now owns a spend-gated Responses SSE loop: it streams visible assistant text, feeds completed function calls and outputs through that executor, continues with the provider response ID, and durably persists final text, exact tool-derived evidence, and continuation state. It fails closed before mutation without live authorization and transparently uses the existing local grounded answer in the product. Official Responses `response.output_item.done` and Realtime `response.function_call_arguments.done` adapters enter the same executor and return the correct provider-specific function output. Realtime's browser data channel returns those outputs when such an event arrives. Successful and failed activity appears chronologically in Conversation with plain-language labels and no raw provider IDs at desktop or compact widths. A model-generated write is held as `Confirmation required`; the exact provider call only mutates after the professional chooses the specific action, persists a second `explicitUserIntent: true` record, and replaces the pending card with the successful result. The successful result returns to the same open Realtime channel and requests the next spoken response. Durable diagnostic details remain available without leaking internal tool names into the Conversation. Realtime now preserves capture-only mode for Add Source and adds center-column speech conversation configuration with audio playback, automatic tools, interruption, visible assistant transcript, and durable provider-spoken turns. Provider-backed Responses text and Realtime speech, grounded reads, interruption, confirmed write, spoken output, and durable provenance are all live-proven.
**Current gate:** The P0 screen-composition and canonical-language rebuild, bounded Realtime capture UI, constrained semantic Excalidraw Map, version-aware Outputs history, contextual original-brainstorm reveal, usable Style selection, and completed Storyboard-to-Video handoff are implemented. The root design system is reconciled to that exact no-tabs interface and now has a regression test against legacy vocabulary and actions. Dismissed orientation remains available through one quiet `How WorkshopLM works` entry inside the Workshop sheet; it explains Capture → Shape → Deliver, source tracing, and the two sign-offs without adding persistent navigation or competing with the current action. Transient sheets now take initial focus, contain Tab navigation, close with Escape, restore the invoking control, and preserve an assertive error notice without removing the current work. Style now analyzes a public website's page and linked stylesheets for reviewable name, palette, font, and brand-asset candidates; the professional can correct every finding before `Use this style` persists the website-sourced `DESIGN.md` tokens. Its role assignment rejects decorative text-color traps, chooses a readable Text/Background pair, favors a distinctive brand Accent, and refuses any saved palette below 4.5:1 text contrast. A fresh production-browser run against Mozilla's live public site found 43 colors, eight font candidates, five assets, and four stylesheets, then produced a readable black/white/blue editable deck in 1.64 seconds after reaching the first grounded Map in 0.62 seconds; these are controlled local automation timings, not uncoached validation or Codex in-app-browser evidence. OpenAI.com separately blocked automatic review and correctly exercised the clean-default recovery path without losing the Map. Invalid addresses, private-network targets, redirect abuse, blocked scans, JavaScript-only shells, and pages without useful findings now fail into privacy-safe named states while the Map remains usable and `Try again`, `Set manually`, and `Use a clean default` remain available. Brand assets remain unselected until the professional opts in; selected PNG, JPEG, WebP, or safe SVG assets pass public-network, size, signature, dimension, and SVG-content validation, are copied locally by content hash, and are reverified before a renderer embeds them. Editing a saved Company Style now creates an immutable numbered revision; existing Workshops retain their prior snapshot until the professional explicitly applies the latest version, which truthfully marks dependent Outputs `Needs update`. Exact manual rules remain available, Client pitch, Board presentation, and Team workshop persist as Intent Profiles, and secondary brand details remain collapsed until requested. Adding a new meeting now expands the Map, invalidates the approved Brief and current downstream work, preserves the reusable Style, and makes one reapproval plus `Update outputs` produce a new deck version traced to the new Source; stale history remains visible without making the new version appear stale. While a local render is queued or rendering, the browser silently refreshes the Workshop; once it succeeds, `View video` replaces the obsolete creation action and the real local MP4 appears in Outputs and a responsive focused player. Every successful render now becomes an immutable `Video · Version N` record with its own MP4, provenance sidecar, source-traceable build record, input versions, hashes, and stable routes; upstream changes keep prior versions visible as `Needs update` rather than overwriting or hiding them. The generated build record follows raw transcript → active Sources → Map → approved Brief and Style → hashed Outputs → Video, then appends actual logged milestones, Build Week commits, available Codex task IDs, provider-evidence counts, elapsed time, and honest limitations. It appears only after `Show original` and ships as HTML and JSON inside the verified submission set. The Map persists direct movement, resizing, and text edits through typed graph history, exposes source trace, and restores each operation through WorkshopLM Undo; its native Excalidraw product chrome is suppressed so the official Apps in ChatGPT shell remains the only surrounding interface system. Outputs now preserve real previews and version history with type, freshness, and source coverage while keeping creation in the single header action; the newest Presentation owns the one full-width hero position and supporting outputs follow in workflow order. Focused Outputs preserve the artifact-specific source count, title, type, and version; `Show source` is the one dominant action and the external action names the file or video it opens. Audio Overview now joins Production as a grounded, editable, immutable-version Output with exact source reveal, local playback/download, duration/hash/model/voice provenance, AI-voice disclosure, stale propagation, shared text/voice/plugin creation, and recoverable provider failure. The focused Video exposes one `Show original` action that keeps the finished work visible beside an honestly labeled transcript or sanitized excerpt and its its resulting Outputs. Storyboard panels now bind the exact image-panel ID and version reviewed on screen; version-changing image regeneration revokes approval, and the local renderer stages those same bytes for HyperFrames. Five independent cold review passes now identify the current object, source scope, and next action on Map, Brief, Outputs, Storyboard, Sources, and focused Outputs at the verified widths with no critical navigation failure. A repeatable production-browser capture now records that complete fixture seam as a 58.0-second, 12-beat editorial draft with real sanitized provider images, hashes, exact gate state, and explicit fixture limitations. The meta-demo fixture now produces a real five-slide professional narrative—outcome, problem, traceable proof, and two-sign-off recommendation—in both HTML and editable PowerPoint; role-first claim selection, preserved supporting copy, and contrast-aware Accent foregrounds prevent the specific failures found in the visual audit. Judge-facing drafts use the canonical product language and an evidence-gated claim ledger. The real privacy-safe Codex in-app-browser doorway is captured and integrated into the rough cut; final publication remains open until the founder recording and public links exist. Provider-backed GPT Image 2 gallery media, Responses text, standalone Cedar Audio Overview, and the complete Realtime read/interruption/confirmed-write transport are live-verified. The remaining recording gate is the founder's physical-microphone turn in the Codex in-app browser.
**Historical provider-risk record:** The detailed pre-spend controls and estimates below remain useful architecture evidence, but every statement that a paid request had not run is superseded by the 2026-07-16 live-product truth above. Terra Map, GPT Image 2, Cedar narration, and Realtime WebRTC have now run successfully inside the authorized ceiling. The configured API key authenticates and can retrieve `gpt-image-2`, `gpt-4o-mini-tts`, `gpt-realtime-2.1`, and the GPT-5.6 variants `gpt-5.6-luna`, `gpt-5.6-terra`, and `gpt-5.6-sol`. The bare `gpt-5.6` endpoint returns `404 model_not_found` for this account even though the current public model page describes it as a Sol alias. The locked runtime uses an operation-level GPT-5.6 routing policy. The benchmark and live operator retain their explicit request ceilings, durable attempt records, state-preserving retry fingerprint, input limits, and fail-closed authorization. Exact dollar debit still is not returned by provider responses; do not infer a more precise spend claim than the recorded request envelope supports.
**Known host risk:** Native Codex/ChatGPT task and voice-turn persistence remain unproven and must not be claimed, but that host sync is no longer the product path. WorkshopLM owns browser Conversation. The full Realtime provider path is live-proven in controlled Chrome for spoken response, grounded reads, interruption, a confirmed write, and durable conversation/tool provenance. The Codex in-app browser reached the same local product and token route but kept microphone permission at `prompt` during automation; the founder must grant it interactively before recording.

**Hard schedule gates:**

- **July 20, 2026, end of day CT:** feature-complete. No new features merge after this point.
- **July 21, 2026:** verification and submission only. Target submission by 12:00 PM PT, five hours before the 5:00 PM PT deadline.

## Objective

Implement and verify the complete locked WorkshopLM product for OpenAI Build Week in a pnpm/Turborepo monorepo.

Deliver the full WorkshopLM scope through one exceptionally intuitive **Capture → Shape → Deliver** path:

1. Start inside WorkshopLM's center Conversation column with text or voice and select the Sources that matter from the persistent left column.
2. WorkshopLM turns that material into an editable, source-grounded Map.
3. Approve the Map as the Brief and choose a Style from a website or uploaded brand assets.
4. Create source-traceable Outputs: a presentation, infographic, Audio Overview, coherent image set, and editable storyboard.
5. Review and approve the storyboard, then create the narrated local video.

Keep citations, exact brand rules, version history, and clear `Needs update` states throughout the work. Use WorkshopLM to create its own truthful hackathon submission and final meta-demo video. Target **Work & Productivity**. Keep Education/Learning mode, general-purpose design-tool scope, and unsupported production claims out of scope.

Completion requires verification of the live product, responsive UX, core tests, OpenAI integration behavior, and judge-facing submission artifacts against the current research and hackathon rules.

## Continuous product-quality track

WorkshopLM should preserve the clarity that makes NotebookLM useful while completing the professional workflow NotebookLM stops short of: **capture the thinking, shape it visually, and ship work worth putting your name on**. Improve the product against these durable rules as implementation continues:

- [x] Make voice and text feel like two inputs to the same Conversation, not separate modes. Either path produces a durable Source, grounded Map updates, and one clear next action without asking the professional to understand models, retrieval, or orchestration. The 2026-07-16 live WebRTC proof persisted the spoken Source/turn and exact provider/tool provenance through the same center-column Conversation used by text.
- [x] Keep the NotebookLM information model professionals already understand—one Workshop, a clear Source set, a central thinking surface, and generated work—while removing its style inconsistency and terminal-output limitations.
- [x] Keep one dominant current object. Sources remain available without owning the canvas; Create remains visible without becoming a dashboard; secondary detail moves into contextual sheets and focused object views.
  - 2026-07-16 compact-navigation pass: removed the last persistent five-item object strip and replaced it with one `Views` sheet assembled from the locked SideSheet, ListGroup, ListRow, and ListRowAction primitives. Chat, Map, Brief, Outputs, and Storyboard remain one interaction away; incomplete objects say `Available later`; focus returns to the trigger; and the current object gains the reclaimed vertical space.
- [x] Preserve the five-second test at desktop and compact widths: the professional can name what they are viewing, which Sources support it, and what to do next.
- [x] Treat the Excalidraw Map as the editable thinking layer, not an exposed drawing tool. WorkshopLM owns clustering, evidence colors, source links, fitting, persistence, and undo; the professional directly moves, resizes, edits, and traces ideas without learning Excalidraw mechanics.
- [x] Apply the current `DESIGN.md` consistently but appropriately. The Map may inherit its paper, ink, and accent while retaining semantic verified/derived/creative colors. Presentations, infographics, GPT Image 2 prompts, storyboards, and videos inherit the full typography, palette, layout, image-treatment, motion, asset, and negative-rule system.
- [x] Treat the approved Storyboard as the editable contract for video. HyperFrames receives the exact current `DESIGN.md` and token version, approved image versions, grounded narration, timing, and Cedar audio; it lints and inspects the composition before deterministic local rendering. Users see one plain-language `Create video` action and recoverable errors, never CLI or timeline mechanics.
- [ ] Make all generated work editable enough for the professional's final 10 percent. Preserve source trace, version history, and calm `Needs update` behavior through regeneration rather than overwriting prior work.
  - 2026-07-16 finishing-action pass: focused Presentation and Infographic now lead with the editable PowerPoint download instead of a duplicate source button; focused Video leads with `Edit storyboard`. Source trace remains claim-by-claim directly below the artifact, and full-size preview, original reveal, history, staleness, and old versions remain intact. Production-browser proof passed at desktop, compact, and mobile widths. Keep this item open for founder-Source dogfood and intended-audience confirmation of the complete last-10-percent loop.
  - 2026-07-16 Video revision path: the focused Video now exposes `Edit storyboard` beside source trace, original brainstorm, and standard MP4 actions. It returns directly to the approved panel editor without mutating or bypassing approval; any saved panel change still revokes approval, stales Video, and requires the existing review → approve → render sequence. Desktop, compact, and mobile browser proof verifies the action and the intact approval boundary. Keep this item open for founder-Source dogfood and intended-audience confirmation of the complete last-10-percent loop.
  - 2026-07-16 image revision pass: `Request replacement` now opens a focused `Replace image` sheet where the professional describes exactly one visual correction. The current image remains visible while pending; the revision is bounded, versioned, source-preserving, Style-preserving, and proven to reach only the selected GPT Image 2 request. Keep this item open for final founder-Source dogfood and intended-audience review of the complete Output set.
  - 2026-07-16 complete-Output editability audit: Presentation and Infographic hand off editable PowerPoint, Sketch hands off editable SVG, Audio Overview edits source-linked script sections, images accept one directed replacement, Storyboard edits remain the Video contract, and Video returns to that editor. The audit found one real gap: Sketch regeneration preserved bytes but not a reopenable state edge. Sketch now keeps immutable prior records, version-specific preview/download routes, focused history, and one family-level Workshop count. Keep this item open only for the founder-Source dogfood and intended-audience confirmation already named above.
- [ ] Judge every visible Output against “would a serious professional send this?” Repair weak hierarchy, generic imagery, awkward narration, inconsistent art direction, and templated layouts before adding output breadth.
  - 2026-07-16 Presentation Version 6 pass: the real provider-backed deck was rejected for a visibly clipped opening claim and a sparse process slide that treated sequence as decoration. The current renderer extracts the complete spoken transformation into headline and support, recognizes explicit three-stage flow, and renders the stages as editable process cards in both HTML and PowerPoint. The live deck passed archive, LibreOffice/PDF, text-extraction, full-resolution contact-sheet, and focused workbench review. Keep this continuous gate open for the intended-audience `Send` or blocking `Revise` decision.
  - 2026-07-16 process-hierarchy refinement: a second visual audit found the process cards structurally correct but still under-informative. The renderer now gives Capture, Shape, and Deliver one quiet definition apiece in HTML and editable PowerPoint while keeping the slide's Source trace unchanged. A dedicated two-slide PowerPoint survived LibreOffice/PDF conversion and full-resolution inspection with no clipping or duplicate text. Keep this continuous gate open for intended-audience judgment.
  - 2026-07-16 Infographic Version 4 pass: the real provider-backed one-page visual was rejected for the same clipped spoken idea, timestamp-heavy voice label, and process-as-ordinary-text failure. The current artifact preserves the complete idea, plain `Voice brainstorm` provenance, and an editable visual sequence. A strict browser baseline then exposed and repaired a CSS collision between Presentation layout names and embedded Infographic cards. Archive, PDF, extracted text, full-resolution output, desktop/compact focused workbench, all 29 browser states, gallery, and rough-film evidence now pass. Keep the continuous gate open for founder-Source and external review.
- [ ] Continue simplifying language. Use familiar nouns and verbs—Sources, Map, Brief, Style, Presentation, Storyboard, Video, `Show source`, `Approve brief`, `Create outputs`—and remove implementation terms, file names, model names, IDs, and duplicated actions from the primary UI.
  - 2026-07-16 no-tab pass: the mobile `Chat / Map / Brief / Outputs / Story` control row is retired. `Views` opens one plain-language Workshop sheet, eliminating five persistent labels while preserving every destination and its readiness state.
  - 2026-07-16 focused-action pass: removed the duplicated header-level `Show source` from Presentation, Infographic, and Video. Exact source access now has one consistent home in the adjacent claim trail, while each header names one professional action: `Download PowerPoint` or `Edit storyboard`. No capability or trace edge was removed.
  - 2026-07-16 object-first metadata pass: the default Outputs gallery and Create rail no longer repeat `Version`, artifact type, or Storyboard revision beside names that already communicate the object. Cards now foreground only source scope and freshness; the Brief shows its Source count; Video shows its scene count; and the ready total now counts every actually shareable deliverable, including approved Storyboard, current Audio Overview, and current Video. Revisions remain explicit in focused objects, accessible names, and Version history. The complete 29-case production-browser suite and refreshed desktop, compact, mobile, provider-backed, gallery, and film evidence passed.
  - 2026-07-16 live-Output pass: removed the fallback model name and timestamp from visible Presentation/Infographic citations, fixed singular Source counts, and moved prior Presentation, Infographic, Audio Overview, and Video versions behind the focused object. Exact locators and every immutable version remain available without crowding the main gallery. Keep this continuous item open through final founder-Source and film review.
  - 2026-07-16 voice-language pass: normalized capture-only and founder-recording Sources to plain titles and origins across navigation, replaced the fixture's `Voice capture fallback` Map card with `Voice stays available`, and retained the exact raw locator only inside the evidence view. The strict suite now separates professional language from inspectable trace data explicitly.
  - 2026-07-16 privacy-and-plain-language pass: the workbench `Add source` sheet now sends pasted notes as `private`, identical to the first-run path, and a browser-level request assertion prevents regression to `sanitized`. Replaced `source-defensible`, `grounded thinking`, and provider-evidence errors with plain descriptions of sourced claims, organized ideas, selected Sources, and a transcript retry. The AI-voice disclosure now tells the professional exactly what can be reviewed before audio is created. Keep this continuous item open through the founder-Source film pass.
  - 2026-07-16 public-package privacy pass: submission export now refuses every active private Source before it reads or copies shareable evidence. Founder imports stay private unless the operator deliberately supplies `--share-founder-source`; the raw staged recording and transcript are Git-ignored. This preserves the meta-demo reveal without making public sharing an implicit consequence of local capture.
  - 2026-07-16 source-truth pass: replaced user-visible `Verified` with the narrower `Sourced` wherever WorkshopLM means “this claim retains a Source edge.” The Brief now names `Sources behind this brief`; Chat names Sources directly; exact locators remain one interaction away. Closing a contextual sheet also restores both focus and scroll, preventing the focused artifact from jumping under the header. All 29 production-browser cases passed after explicit desktop, compact, onboarding, Brief/Style, Map, and Video review. Keep this continuous item open through the founder-Source film pass.
  - 2026-07-16 final internal-language pass: the workbench now calls its right rail `Create`, its tool-activity channel `Workshop`, and its canvas simply `Editable Map`. The original-brainstorm reveal says `Became finished work` and measures time to the first `finished Output` instead of exposing production-system language. Refreshed desktop, compact, and mobile reveal baselines passed visual inspection, and all 30 production-browser scenarios passed. Keep this continuous item open through the founder-Source film pass.

The implementation may add composites only when the official Apps in ChatGPT library lacks the required pattern; those composites must be assembled solely from its verified primitives and inherit its spacing, typography, color, elevation, focus, and responsive behavior.

## Definition of fully implemented

This goal is not complete when the architecture, shell, mocks, or isolated model calls exist. It is complete only when one coherent local product demonstrates all of the following:

1. The WorkshopLM plugin opens one self-contained three-column Workshop in the Codex in-app browser or supported local browser surface.
2. WorkshopLM's center Conversation column accepts text or Realtime voice, persists the durable transcript as a Source, and remains visibly connected to the current Map, Brief, or Output. Codex is not the end-user chat interface.
3. Local files, websites, and sanitized meeting notes become searchable Sources with an inspectable path from claim to exact source excerpt.
4. GPT-5.6 turns the conversation and selected Sources into an editable Map with undo, evidence states, and a regenerable Sketch.
5. `Approve brief` creates inspectable `FRAME.md`; a website or manual brand assets create reviewable, versioned `DESIGN.md` and Style data.
6. `Create outputs` produces a source-traceable presentation, infographic, Audio Overview, coherent GPT Image 2 image set, editable storyboard, and narrated local HyperFrames video.
7. Storyboard approval, version changes, `Needs update` states, retry, cancel, and partial success work across the real end-to-end path—not only in isolated tests.
8. `How this was built` traces the real submission from raw brainstorm through Sources, Map, Brief, Style, Outputs, build evidence, and measured time to first output.
9. The public demo video is under three minutes and shows the real flow clearly enough that judges do not need to install the local plugin.
10. The repository passes the documented checks, the live demo is recorded from the intended surface, and every judge-facing claim matches captured evidence.
11. Every judge-visible control and container—including the three-column shell, Conversation composer, Sources rail, Production rail, inspectors, collapse controls, and responsive adaptations—comes from the verified Apps in ChatGPT Figma inventory or a documented WorkshopLM composite made only from those primitives. A first-time professional can identify the current object, source scope, production state, and next action within five seconds, without seeing internal file names, IDs, implementation terms, or duplicate controls.

All items above are committed scope for this hackathon version. Fallbacks may change implementation mechanics, but may not silently remove the user-visible capability they preserve.

## Integration priority

The seam **voice/source capture → grounded Map → approved brief → branded outputs → approved storyboard → rendered video** outranks breadth. If the daily acceptance run is red, repair the seam before merging new features; resume breadth when it is green.

## Product identity

- **Name:** WorkshopLM
- **Tagline:** Turn raw thinking into finished work.
- **Professional promise:** From your meetings and documents to polished Slides and supporting work you can defend, with every claim traced to its source.
- **Primary user:** A consultant, strategist, or enablement lead producing client-facing or leadership-facing work every week.
- **Wedge deliverable:** Grounded, branded, source-defensible Slides. Infographic, images, Audio Overview, Storyboard, and Video are coordinated Outputs, not hidden secondary features.
- **Internal thesis:** NotebookLM, but better for professional production and built with OpenAI.
- **Judge-facing shorthand:** WorkshopLM is NotebookLM for finished work—built on the OpenAI API.
- **Track:** Work & Productivity

The NotebookLM association is intentional category shorthand for hackathon judging. WorkshopLM must not imply affiliation with Google or present itself as an official OpenAI product. Keep Google's marks out of the project title, video title, thumbnails, and product UI; use the comparison only in explanatory copy or narration.

## Product quality operating order

The hackathon is a deadline and distribution event, not the product definition. Sequence work by the professional replacement test:

1. **Wedge:** make grounded, branded Slides the hero workflow without presenting WorkshopLM as a slides-only product. A cold professional should understand the promise in 20 seconds, see their Sources become an organized Map within two minutes on the live path, and reach shippable first Slides from their own material within 15 minutes.
2. **Send-it bar:** grade every Output with `Would I send this?`, beginning with Slides. Supporting Images should belong in serious professional work; Storyboard and Video should be leadership- or LinkedIn-ready; every Output needs a credible last-10-percent editing path.
3. **Trust:** one consistent gesture traces any factual claim to its exact source sentence. Staleness is visible and actionable, the two approvals have clear consequence, unverified material stays labeled, and AI voice stays disclosed.
4. **Return visit:** adding a Source refreshes the Map and honestly stales dependent work; a second Workshop starts without friction; Style Libraries carry across Workshops; the weekly meeting → refreshed Brief → regenerated Slides and supporting Outputs rhythm retains brand and trace.
5. **Reality ladder:** the Build Week submission is dogfood zero. Dogfood one is a real external professional deliverable; then three to five uncoached representative professionals use empty Workshops; graduation requires one person outside the project to return and use WorkshopLM twice.

Provider proof and the final video remain deadline-critical, but additional output breadth never outranks a failing send-it, trust, or return-visit test.

### Professional product gates

These gates measure replacement value, not implementation breadth. Automated checks may prove mechanics; they do not substitute for the named human or live-path evidence.

#### First five minutes

- [x] A first-time professional can identify the current object, source scope, and next action within five seconds across the required recorded screens. This is screenshot-orientation evidence, not uncoached empty-Workshop use.
- [ ] On the provider-backed path, a professional can add their own material and see the grounded Map organize it within two minutes.
  - 2026-07-15 capture-first browser proof: a genuinely empty local Workshop in the Codex in-app browser now moves directly from the chosen outcome to voice/notes/URL/PDF intake; Company Style no longer blocks the first Source. One realistic pasted meeting note became three separately grounded ideas, and the complete source card plus all three claims fit inside the 1280×720 center canvas with both context rails open. At 390×844, the same Map became three readable verified cards with document width equal to viewport width. Brief approval then made `Choose style` the one next action. This proves the no-spend product order and responsive deterministic path, not the still-open provider-backed two-minute gate.
  - 2026-07-16 controlled provider proof: five shareable AI Collective evidence blocks became a six-node, five-edge `gpt-5.6-terra` Map in 5.957927 seconds from the normalized Source timestamp under a fail-closed one-request ceiling. The live Map passed 1200px and 760px workbench inspection with no horizontal overflow. This clears the provider/product latency floor but not the named gate: no uncoached professional supplied their own material, and the recovered timer excludes the earlier ingestion interval.
- [ ] From an empty Workshop and the professional's own material, the first editable, source-defensible Slides are ready within 15 minutes. Record elapsed time and every manual repair.
  - 2026-07-15 mechanical proof: a new empty Workshop accepted fresh pasted meeting notes, derived the Source title, built the grounded Map, approved the Brief, applied a Style, created Outputs directly from the Brief, and opened an editable `.pptx` presentation in 2.6 seconds of automated production-browser time with no manual repair. The same source field now routes pasted notes, a public URL, or an absolute local PDF path without a mode picker. The human gate stays open until a representative professional repeats this with their own material and no coaching.
  - 2026-07-15 composition proof: the isolated Codex-browser clean start now turns six realistic verified claims into six separately editable, claim-backed Map ideas instead of one oversized Source summary. The approved Brief selects one concise outcome and three exact evidence sentences, each with an attached locator that opens the precise source text. This closes the deterministic first-draft composition defect; it does not close the provider-backed or uncoached-professional gates.
  - 2026-07-16 controlled provider proof: the same external-use run reached its first editable PowerPoint in 5.983927 seconds with no repair before generation. Inspection rejected the draft because locator metadata entered one slide and a transformation clipped awkwardly. The production seam now excludes metadata paragraphs at ingestion and deck selection and renders `becomes X and Y` as grammatical headline plus support. Accepted Version 3 regenerated with zero additional provider requests and passed a five-page LibreOffice/PDF round trip plus desktop/compact focused-output review. The uncoached professional-own-material and external `Send` gates remain open.

#### Send-it quality

- [ ] A cold professional returns `Send` on the external-use Slides candidate or names the first required revision; apply that revision and repeat until the Slides clear the bar.
  - 2026-07-16 provider-visual integration: the current authorized-sample Presentation Version 4 now embeds the approved GPT Image 2 hero in HTML and editable PowerPoint, records the exact image batch/panel/version/hash dependency, and becomes stale when that panel is replaced. The five-page 16:9 LibreOffice/PDF round trip and live Outputs gallery passed visual inspection after the renderer mapped system-font aliases to portable Arial. This closes the disconnected-image and PowerPoint-font defects; it does not replace the external `Send`/`Revise` decision.
  - 2026-07-15 internal quality pass: LibreOffice/PDF inspection exposed empty layout furniture and repeated copy when a selected claim had no separate supporting sentence. Sparse statement, split, proof, and recommendation slides now use intentional claim-only compositions; the client-facing label is explicit, the split layout has a stable pale evidence rail, role labels describe the slide's job, and the recommendation no longer repeats its headline as body copy. The five-slide fixture deck, editable `.pptx`, source footers/notes, responsive UI previews, and `artifacts/deck-send-it-audit-2026-07-15.png` are verified. This improves the candidate but does not close the cold professional `Send`/`Revise` gate.
  - 2026-07-15 meta-demo audit: the acceptance fixture had regressed to a cover plus one repeated claim even though generation tests passed. It now produces a five-slide outcome → problem → proof → recommendation narrative, retains supporting clauses instead of dropping them after headline clipping, and chooses readable foreground text for both light and dark Accent colors. HTML and LibreOffice-rendered PowerPoint contact sheets passed internal visual inspection; external `Send`/`Revise` evidence is still required.
  - 2026-07-15 brand-fidelity repair: reviewed logo dimensions now reach the renderer, and editable PowerPoint fits wide wordmarks, square marks, and tall symbols inside bounded cover/infographic regions without changing their proportions. The external-use deck rebuild also detects its selected SVG from file content instead of trusting a misleading `.png` filename. The refreshed five-slide PDF/contact sheet remained visually clean, the PowerPoint archive contains a real SVG with `noChangeAspect`, and its 515472×621792 EMU frame preserves the source mark's 700×844.38 aspect ratio. This closes a deterministic Style-to-deliverable defect, not the cold professional gate.
  - 2026-07-15 Intent Profile repair: Client pitch, Board presentation, and Team workshop had persisted correctly but changed only the cover label. They now drive distinct HTML and editable-PowerPoint systems over the same grounded Brief and Company Style: Client uses spacious editorial contrast, Board uses conservative paper/ink framing and decision language, and Team uses action language, an energetic cover, protected logo contrast, and a lighter scan-first working slide. `pnpm dogfood:intents:build` produces the three five-slide editable decks plus a nine-frame comparison and hashes under `artifacts/spikes/intent-profile-review-2026-07-15/`. Visual inspection caught and repaired a clipped Board label, an invisible same-color Team logo, and a dark Team middle slide before acceptance. This proves deterministic profile fidelity, not external send approval.
  - 2026-07-15 infographic profile proof: the same executable profile contract now reaches the one-page infographic in HTML and editable PowerPoint. Client retains the source-defensible editorial grid, Board uses restrained top framing and leadership-evidence language, and Team uses a warmer card-based action brief. `pnpm dogfood:intents:build` now adds all three one-page variants, individual review frames, `infographic-contact-sheet.png`, and distinct hashes to the checked-in comparison manifest. All variants preserve the same four grounded claims and visible citations. This closes the deterministic profile leak across the two editable professional outputs; it does not close the human `Send` gate.
  - 2026-07-15 current-renderer evidence sync: after the three-metric and populated-split repairs, `pnpm dogfood:intents:build` regenerated every checked-in Client pitch, Board presentation, and Team workshop deck and infographic in HTML, editable PowerPoint, PDF, and review-image form. The refreshed contact sheets show all three profiles preserve the promoted `180+ / 40+ / 400+` evidence hierarchy and four-week plan without reverting to the removed decorative rail. All 18 manifest output hashes, six PowerPoint archives, and six PDFs passed independent verification. This keeps the proof set current; it does not replace external `Send` evidence.
  - 2026-07-15 external dogfood revision: the first five-slide chapter-launch candidate received an internal `Revise`, not a cosmetic pass. It was a polished promotional teaser without an executable plan, operating commitments, or a decision. The rebuilt nine-slide editable deck adds source-backed context, a visibly derived four-week plan, trust and sustainability commitments, three explicit decisions, and one recommendation. New plan and decision layouts remain editable in PowerPoint; the archive, nine-page LibreOffice/PDF round trip, source notes, derived labels, and full-resolution review frames passed internal inspection. The real cold professional `Send`/`Revise` judgment remains open.
  - 2026-07-15 evidence-hierarchy repair: full-resolution review of the nine-slide deck found that the strongest proof slide visually promoted only `180+ chapters` and `40+ countries` while burying `400+ organizers` in body copy. The renderer now supports balanced one-, two-, and three-metric evidence compositions in responsive HTML and editable PowerPoint. The rebuilt external deck promotes all three verified figures at equal hierarchy, keeps the narrative support and quiet source trace, and passes archive, nine-page PDF, extracted-text, renderer-test, and visual-review checks. This improves the candidate; only the intended professional audience can close `Send`.
  - 2026-07-15 template-furniture removal: contact-sheet review then exposed two populated split slides spending roughly a quarter of the canvas on giant decorative page numbers. Populated split slides now pair the claim and supporting context across a restrained Accent divider in HTML and editable PowerPoint; only intentionally sparse editorial slides retain the large-number treatment. Full-resolution review confirms both external slides use the reclaimed space without overflow. The intended-audience gate remains open.
  - 2026-07-16 qualitative-evidence refinement: full-sequence review found the trust slide spending a third of its canvas repeating the source label instead of making the two operating standards scannable. Qualitative proof blocks with explicit items now render editable numbered commitment cards in HTML and PowerPoint. The rebuilt slide pairs one synthesis sentence with the conduct and privacy obligations, keeps the quiet policy citation and exact notes trace, and removes duplicated body copy. The cold-review packet and all three Intent proofs now carry the stronger renderer; external `Send`/`Revise` remains open.
  - 2026-07-16 review-version integrity: every packet and downloaded response now carries a content-derived Review ID bound to the current PDF. `pnpm dogfood:review:ingest` verifies the packet PDF/PowerPoint, current source deck, response ID, exact decision, reviewer role, and blocking explanation before preserving returned evidence. Untouched templates, stale packets, and unexplained revisions fail closed; reviewer identity remains explicitly unauthenticated. This protects the gate but does not supply the missing professional response.
  - 2026-07-16 cold-review handoff: `pnpm dogfood:review:build` now produces one deterministic, privacy-safe ZIP containing the current nine-slide PDF, editable PowerPoint, contact sheet, manifest, no-JavaScript feedback fallback, and a responsive `START-HERE.html`. The page provides no project explanation before the decision, asks only `Send` or the first blocking `Revise`, validates a revision explanation, and downloads feedback locally without an account, analytics, form endpoint, or automatic transmission. Desktop and 390px Codex-browser checks passed with no horizontal overflow or console warnings; all six packet links, archive contents, document evidence, fixed hash, and privacy scan passed. This removes review logistics, not the need for a real reviewer.
- [x] The first real GPT Image 2 batch contains deck-usable diagrams, concept visuals, or section art—not generic illustration—and passes a six-panel coherence review.
  - 2026-07-15 zero-spend readiness: the six planned panels are no longer hardcoded demo scenes. Each is derived from the approved Brief, assigned to the strongest matching evidence, and retains its exact claim/source edge through generation, retry fingerprints, the image manifest, and per-image submission assets. Hero concept, systems diagram, evidence chain, decision visual, storyboard sequence, and section art share one reference hash and explicit professional review rubric. The later July 16 live run generated all six panels from this contract; full-set inspection accepted their shared palette, lighting, composition, and deck-usable roles without selective regeneration.
  - 2026-07-15 review loop: the focused Image set now names all six professional visual jobs, shows the approved grounded idea beneath each panel, traces each panel to its exact Source, opens generated bytes directly, and requests only the failed visual. A replacement visibly returns to pending, revokes Storyboard approval, and rebinds the finished new image version before the professional can approve Storyboard again. This proves the last-10-percent control path, not provider visual quality; the real-generation gate remains open.
- [ ] The provider-narrated Video clears a leadership or professional social-post review for pacing, voice, and visual coherence.
  - 2026-07-16 motion and contrast pass: internal review rejected the static first cut even though its composition and narration were coherent. Video Version 4 now uses a local, deterministic GSAP timeline inside HyperFrames for restrained copy entrances, scene fades, and image settles, with hard opacity kills at every clip boundary for correct nonlinear seeking. HyperFrames passes with zero lint or layout errors and all 25 contrast checks; twelve sampled frames, the current product screen, and the rebuilt 2:20 rough cut passed internal visual inspection. The intended leadership or professional social-post review remains open.
  - 2026-07-16 film-caption pass: full-frame review rejected the rough cut's render proof because its narration-derived lower third ended in a visible ellipsis. Every shot now owns a short editorial caption independently from its spoken script, the verifier enforces presence and an 80-character ceiling, and the rebuilt 1280×720 contact sheet shows all ten complete statements. The 2:20 Cedar timing, eight-ready/two-blocked truth state, and provider evidence remain unchanged; the external taste review stays open.
  - 2026-07-16 shot-alignment pass: a second full-frame audit rejected three narration/visual mismatches caused by holding the final frame of short captured actions. The film plan now declares a bounded end holdback for each captured beat, the verifier constrains it to 0.2–2 seconds, and the compositor freezes the intended evidence state. Review frames now pair evidence/edit with the claim inspector, first approval with the approved Brief, Style with the applied Style version, and second approval with the approved Storyboard. The external taste review stays open.
  - 2026-07-16 objective film-QC pass: repeated 12–18 second frozen proof shots failed the send-it bar even though the Cedar narration and screen alignment were correct. The compositor now applies a quiet 6% editorial push with horizontal drift to all ten shots, and packet verification binds that treatment. Black-frame detection found none; loudness remains -22.9 LUFS with -2.9 dBTP; and the only remaining detector hit is a borderline 3.03-second low-motion tail on the blocked founder-capture placeholder. The refreshed contact sheet preserves readable UI, captions, and evidence hierarchy. Keep this item open for the real founder replacement and intended-viewer taste review.
  - 2026-07-16 final-preview truth pass: clean-final preview generation now deletes prior outputs before work, so a missing current submission package cannot masquerade as a freshly rendered meta reveal. The Video proof frame uses the inspected provider render, and the reveal calls its bounded founder text a verbatim excerpt while keeping the full hash-bound Source in WorkshopLM. Current preview, submission package, packet gate, and repository checks pass; the founder and external taste gates remain open.
- [x] Every primary Output has a credible last-10-percent path: editable PowerPoint for the presentation and infographic, in-product panel editing for Storyboard, direct image files plus selective regeneration, and a standard MP4 handoff for Video.
  - 2026-07-15: the Infographic now creates a one-slide editable `.pptx` from the same Style and grounded claims as its HTML preview. The focused Output exposes the same `Download PowerPoint` action as the presentation; its archive, embedded source notes, browser route, LibreOffice round trip, and one-page visual render were inspected. The traced submission Output set hashes and verifies both editable files.

#### Trust and return

- [x] Factual presentation, infographic, Storyboard, and Video claims retain an inspectable path to exact source evidence in the recorded fixture.
  - 2026-07-15 claim-level UI proof: focused Presentation, Infographic, and Video views now list every artifact claim with its exact locator rather than routing `Show source` to only the first claim. Selecting the second Presentation claim in the Codex in-app browser opened its exact `Sanitized fixture · chunk 02` sentence; Video exposes the same direct source action beside `Show original` and preserves the compact playback composition. Image panels and Storyboard panels retain their existing per-item `Show source` gesture.
- [x] Meaningful Source changes make dependent work `Needs update`, preserve historical versions, and offer one clear regeneration action.
- [x] Saved Styles carry across Workshops; a new weekly meeting can refresh the Map, Brief, and presentation without rebuilding the brand system.
- [ ] One representative professional completes the weekly return path on their own material without project-team coaching.

#### Reality ladder

- [x] Dogfood zero: WorkshopLM produces its own traced submission package and exposes defects as product work. The package remains `partial` until provider media and the final public Video exist.
- [ ] Dogfood one: an external-use deliverable is reviewed by its intended professional audience and either ships or generates a concrete revision.
  - 2026-07-15 candidate readiness: internal dogfood rejected the original teaser and produced a materially revised nine-slide AI Collective chapter-launch decision deck. It is ready for its first intended-audience review; no external approval or shipment is claimed.
  - 2026-07-16 review readiness: the candidate now has a self-contained cold-review ZIP with the actual PDF, editable PowerPoint, full visual sequence, and local `Send`/`Revise` feedback capture. The packet is verified and ready to share; this checkbox stays open until an intended-audience professional returns the response.
- [ ] Three to five uncoached representative professionals start from empty Workshops with their own material; every stall becomes a tracked product defect.
- [ ] Graduation: one person outside the project uses WorkshopLM twice without being asked to return.

## Locked first-use onboarding and Company Style contract

This is the next implementation increment. The product already proves the downstream production seam; this contract closes the gap between opening an empty installation and understanding how to reach that seam without coaching.

### Outcome and completion bar

A new professional should be able to open WorkshopLM, state what they are making, add their own material, and reach a useful grounded Map without learning the internal architecture. After the Brief is approved, they create or select a reusable Company Style before Outputs. The user should understand the permanent workflow from one short orientation on the real Map.

The memorable first-use moment is: the professional chooses an outcome, adds the messy meeting or source material, and sees the complete grounded Map before WorkshopLM asks how the finished work should look.

This onboarding is complete only when a fresh local data root passes the empty-Workshop browser path, the company URL produces visibly reviewable exact hex colors and font candidates, a selected Company Style is reused by a second Workshop, orientation state survives reload, and all existing acceptance/demo paths remain green.

### Locked product decisions

- Support multiple reusable **Company Styles**. A consultant may use their own identity for one Workshop and a client's identity for another. One Style may be the default, but no single global identity is assumed.
- Keep the Workshop outcome separate from reusable company identity. `Client pitch`, `Board presentation`, and `Team workshop` belong to the Workshop; changing the outcome does not rewrite the Company Style.
- Offer **Use a clean default for now**. It removes first-use friction, but the user must still have a saved Company Style—website-derived, manual, reused, or clean default—before `Create outputs` becomes available.
- A company website entered for Style analysis is brand evidence only. It never becomes a factual Source unless the user separately adds it through `Add source`.
- Saving Company Style is mutable setup, not approval. The only blocking approvals remain `Approve brief` and `Approve storyboard`.
- Existing Workshops, the deterministic judge fixture, and returning users with durable state must open exactly where they did before. Onboarding appears only for a genuinely fresh installation or a newly created empty Workshop that has not completed setup.
- Use the current Style Library as the implementation foundation. Do not introduce organization accounts, Canva connectors, remote brand management, template administration, or a parallel product shell.

### First-run flow

#### 1. Welcome and outcome

The first screen uses the existing WorkshopLM/OpenAI component system and one dominant action. It says `Turn raw thinking into finished work.` and asks `What are you making?`

- Present three outcome choices: `Client pitch`, `Board presentation`, and `Team workshop`.
- Ask for the Workshop name after the outcome is chosen; offer a useful generated default and permit correction.
- Do not ask for output formats, model choices, audience taxonomies, tutorial preferences, or technical configuration.
- The primary action advances directly to material capture. Back preserves entered state.

#### 2. Add the thinking

Reuse one unified Source surface rather than separate mode cards. It supports:

- `Record voice`;
- pasted meeting notes or transcript;
- a public content URL;
- an absolute local PDF path.

Use short outcome-specific examples only when they help the user choose material. Do not ask for Company Style before the first Source. With one valid Source, enable `Build my Map`. Source ingestion errors stay on this surface with a useful retry or correction.

#### 3. First Map orientation

Land on the real editable Map as soon as it is useful. Show one dismissible orientation card, not a hotspot tour:

> Your Map is ready. Shape the ideas, approve the Brief, then create branded Outputs. Use Show source to trace any factual point back to the material behind it.

The card has one `Got it` action, dismisses durably, and is available later through a quiet `How WorkshopLM works` entry. It must not obscure the Map, the Source count, or `Approve brief` at the recorded desktop and compact sizes.

Fit the complete initial Source-to-claim scene into the visible center canvas while both context rails remain open. The first useful structure must not require panning, zooming, or collapsing a rail to understand.

#### 4. Start the Company Style

After Brief approval, the primary path asks for `Company website` and offers `Find my company style`. The supporting copy states that WorkshopLM will suggest colors, type, and brand assets for review and will not add the website to Sources.

The website path is selected by default for a fresh Workshop. The outcome chosen on Welcome remains the Workshop's production intent and appears here only as context; Style must not ask the professional to choose `Client pitch`, `Board presentation`, or `Team workshop` a second time.

Alternative paths are:

- `Use a saved style` when the Style Library is non-empty;
- `Set it manually` for exact brand rules or a blocked website;
- `Use a clean default for now` for users without a website or brand system.

Submitting a website begins honest review without blocking already-completed Source, Map, or Brief work. Show only honest task states: `Reviewing company.com…`, `Company style ready to review`, `Couldn't review this website`, and `{Name} style saved`. No invented percentages or simulated stages.

#### 5. Review Company Style

Use a focused review surface large enough to judge the result; do not expose raw arrays of URLs or a cramped settings form. The surface says `We found this on {domain}. Keep what is right.` and includes:

- **Identity:** editable Company Style name and the analyzed domain.
- **Brand assets:** visual candidates labeled `Likely logo`, `Wordmark`, `Icon`, or `Other`; the user selects a primary asset, removes false positives, or adds an authorized replacement. Never call a heuristic match the official logo before selection.
- **Colors:** visual swatches with editable, normalized six-digit uppercase hex values. Assign explicit roles: `Accent`, `Text`, `Background`, and optional `Supporting`. A detected list without roles is insufficient.
- **Type:** visible heading and body samples. Candidates are assigned to `Heading` or `Body` and labeled `Detected`, `Available`, or `Needs font file`. Detection never implies installation or licensing. Always show the actual fallback that Outputs will use.
- **Preview:** a small representative title/statement/evidence composition using the current logo, palette, and type choices. Changes update the preview immediately.
- **Additional guidance:** collapsed by default; contains authorized image references, negative rules, and concise usage guidance. Voice/tone inference is not required for this increment.
- **Authorization:** make clear that the user is responsible for rights to selected assets and fonts. Do not represent a detected remote font file as licensed.

The dominant action is `Save company style`. The saved result becomes a reusable Style Library entry and may be selected as the default. Do not use `Approve`, `Lock`, `Visual DNA`, `DESIGN.md`, or other internal language in this surface.

#### 6. Brief-to-Outputs boundary

If the Brief is approved and Company Style is saved/current, show its compact logo, palette, and typography summary with `Edit`, then enable `Create outputs`.

If the Brief is approved but Company Style review is unfinished, the primary action becomes `Review style`. Do not create Outputs from unreviewed candidates. The Style requirement blocks only Outputs; it never blocks Sources, Map editing, or Brief approval.

#### 7. First Outputs orientation

After the first successful Output set, show one dismissible cue:

> Your presentation is ready. Show source traces a claim back to its material. The Storyboard is your second and final approval before Video.

Offer `Open presentation` and `Got it`. Do not create a separate tutorial area or repeat the explanation after dismissal.

### Reuse, versioning, and persistence

- A Company Style stores a durable name, source kind and URL when applicable, palette roles, typography roles and availability, selected local brand assets, references, negative rules, analysis summary, and timestamps.
- Applying a Company Style to a Workshop creates or updates that Workshop's versioned Style snapshot. A Workshop pins the exact saved Company Style revision it uses.
- Applying a saved Company Style preserves the current Workshop outcome. Reusable brand identity must not silently replace the production intent selected for the Workshop.
- Editing a reusable Company Style creates a new revision. Existing Workshops do not silently change. A user explicitly applies the new revision; normal `Needs update` propagation then applies to dependent work.
- The selected outcome/Intent Profile is stored on the Workshop, not inside reusable company identity. Applying the same Company Style to a board presentation and client pitch preserves the brand while changing the output intent.
- Store only user-selected brand assets in the local data root. Validate media type, size, dimensions, SVG safety, and content before persistence. Outputs use the local copy and hash; they never hotlink remote website assets.
- Persist first-use state separately from approval state: welcome completion, Map-orientation dismissal, Outputs-orientation dismissal, default Company Style, and current setup status. Do not encode onboarding as another approval or infer it from unrelated output artifacts.
- Migrate existing local state as onboarding-complete. A clean fixture reset may explicitly choose whether it exercises the canonical demo Workshop or the clean-start onboarding fixture; the two must not contaminate each other.

### Website analysis truth and safety boundaries

- Reuse the existing deterministic public-website analyzer and its private-network, redirect, credential, and content-size protections. It already reads the page and bounded linked stylesheets, extracts exact color candidates, finds font-family candidates, and discovers likely brand assets.
- Improve candidate ranking and review metadata only as required by the visible contract. Do not add a headless browser, web crawler, external connector, or unbounded site traversal for this increment.
- CSS variables, `rgb`, `hsl`, opacity, dark mode, utility colors, favicons, partner marks, and footer marks can create false positives. Every result is a candidate until reviewed.
- Dynamic, authenticated, bot-blocked, malformed, or brand-light websites must fail gracefully into manual or clean-default setup.
- Do not download or execute remote scripts. Do not silently fetch arbitrary font binaries. Never expose local-network state through website analysis.
- Analysis progress and errors must be real. A partially successful scan may show useful candidates with a concise warning; it must not claim a complete official brand kit.

### Minimal implementation surface

Prefer additive changes around the existing Workshop service and Style Library:

- extend Workshop creation/setup with the chosen outcome and durable onboarding metadata;
- extend website Style suggestions with candidate roles, selection, evidence/availability metadata, and an honest analysis status;
- extend saved Style Library entries with reviewed color/type roles and validated local asset records;
- reuse `analyzeWebsiteStyle`, `lockWebsiteStyle`, `lockManualStyle`, `applyStyleLibrary`, and existing source-ingestion actions where their contracts remain truthful;
- keep analysis concurrency small and deterministic. It may be implemented as an observable local task or a reload-safe request flow, but a refresh must either restore the result/status or offer an explicit retry without corrupting the Workshop;
- preserve `canCreateOutputs = briefApproved && currentStyleExists && !currentStyleIsStale` as the meaningful delivery boundary;
- retain `briefApproved` and `storyboardApproved` as the only approval state.

Do not refactor unrelated rendering, plugin, graph, queue, or submission systems unless a directly exercised onboarding dependency requires a small compatible change.

### Acceptance and test gate

- [x] A fresh local data root opens the Welcome flow; existing Workshops and the judge fixture bypass it without state loss or changed default routes.
- [x] Outcome and Workshop name persist across Back, reload, and completion; outcome remains Workshop-specific when a Company Style is reused.
- [x] Entering a company URL starts bounded analysis without adding that URL to Sources or blocking source ingestion and Map creation.
- [x] A deterministic website fixture proves linked-CSS extraction, exact normalized hex values, font candidates, likely assets, editable corrections, palette roles, typography roles, and analysis findings.
- [x] Company Style review displays visual asset candidates, actual swatches and hex values, heading/body samples, font availability/licensing truth, and a responsive live preview.
- [x] Selected remote brand assets are validated, copied locally, hashed, and used by generated work; unselected or invalid assets never enter Outputs.
- [x] Scan failure, no useful findings, dynamic-site failure, invalid URL, redirect abuse, private-network targets, unsafe SVG/media, and unavailable fonts each have tested safe behavior and a usable manual/default fallback.
- [x] `Use a clean default for now` creates a real saved reusable Style and never bypasses the Brief or Storyboard approval gates.
- [x] A second Workshop can select the first Workshop's Company Style in one action while choosing a different outcome.
- [x] Editing a Company Style creates a revision and never silently changes existing Workshops; explicitly applying the revision produces correct downstream `Needs update` behavior.
- [x] Map and Outputs orientation appear once, dismiss durably, remain accessible later, and do not obscure the permanent next action.
- [x] Exactly two blocking approval actions remain visible and enforced: `Approve brief` and `Approve storyboard`.
- [x] Keyboard, focus, labels, error announcements, contrast, reduced motion, and responsive behavior pass at the current desktop, compact, and mobile verification widths.
- [x] Add focused service/domain tests, API/route tests, component tests, clean-start production-browser tests, existing-workshop regression coverage, and one complete onboarding-to-first-Output acceptance path.
- [x] Run and record the narrow suites first, then `pnpm check`, the production-route browser suite, `pnpm demo:e2e`, `pnpm submission:build`, and `pnpm submission:verify`. Update screenshot baselines only after inspecting intentional changes.
- [x] Inspect the finished clean-start path in the production browser with a real public website and representative source material. Record elapsed time to first Map and first editable deck without upgrading that single run into uncoached professional validation.
- [x] Repeat the same clean-start inspection in the Codex in-app browser when that host capability is callable; do not substitute the production-browser run for this host-specific proof.
  - 2026-07-15 Codex-browser proof: an isolated local data root completed outcome → Style → pasted Source → grounded Map → approved Brief → editable presentation in 37.96 seconds of controlled agent interaction, including deliberate waits. The 1280px host surface had no horizontal overflow or console warnings/errors. The run exposed and then verified fixes for a success notice that covered `Show source` and a generic truncated evidence excerpt; the final presentation trace shows the exact approved claim and `Pasted notes · chunk 01`. This is host-path proof, not uncoached professional timing or provider-backed model evidence. See `artifacts/spikes/codex-iab-clean-start-2026-07-15.json`.

## Locked interface contract

WorkshopLM adopts NotebookLM's strongest interaction architecture while expressing every surface through the already-verified Apps in ChatGPT Figma design language:

1. **One three-column Workshop.** Desktop and wide tablet use one stable `Sources | current object | Production` shell. Conversation is a center work object, not a fourth region. Columns may resize or collapse, but their meaning and location do not change during the core flow.
2. **WorkshopLM owns Conversation.** The center column contains WorkshopLM's text and Realtime voice interface, grounded responses, and the current Map, Brief, Storyboard, or focused Output. Codex remains the build, plugin, and launch host; it is not the primary end-user chat surface.
3. **Sources remain visible.** The left column persistently exposes active scope, source type, claim count, selection, add/capture actions, and the selected source or evidence excerpt. A citation reveals the exact excerpt beside the claim under review instead of replacing it.
4. **Production accumulates finished work.** The right column persistently exposes Brief, Style, Presentation, Infographic, Image set, Storyboard, and Video as an evolving production history with honest empty, planned, in-progress, review, current, failed, and needs-update states.
5. **The current object owns the center.** Conversation and the selected work object share the primary region. Focused review expands only when the Brief, Storyboard, or finished Output needs more space; Sources and Production remain available in compact or collapsible form.
6. **One operational progress model.** Capture -> Shape -> Deliver is selectable and stateful. It identifies the current stage, completed prerequisites, blockers, and one obvious next action. The header stays global instead of swapping through the entire workflow.
7. **Two consequential approvals.** `Approve brief` and `Approve storyboard` remain the only blocking approvals. Their prerequisites, consequence, and resulting state are visible in the center and Production column; video creation remains unavailable until Storyboard approval.
8. **Progressive disclosure protects density.** Technical details, version internals, advanced Style controls, and secondary actions stay collapsed. Persistent columns show only information needed to orient, select, verify, or advance the work.
9. **Status earns its space.** Show a state only when it changes what the user can or should do. Do not repeat `Approved`, `Up to date`, source counts, or version labels across columns.
10. **The product renders work, not source files.** Brief and Style are readable product views. `FRAME.md`, `DESIGN.md`, raw Markdown syntax, artifact paths, model names, IDs, hashes, and version mechanics remain available only under technical Details or export.
11. **Mobile adapts rather than shrinks.** Narrow screens use one column plus a sticky object/stage switcher for Sources, Conversation/Map, and Production. No critical function disappears, and the current scope, stage, and next action remain reachable without reproducing three squeezed columns.

`Apps in ChatGPT · OpenAI Official (Community)` remains the only source for product chrome. The three-column decision authorizes a new composition, not a new visual system. Use the verified primitives, variables, text styles, effects, icons, spacing, and display-mode templates from the recorded Figma inventory. If the library lacks a three-column, Conversation, rail, splitter, or responsive pattern, build a named WorkshopLM composite exclusively from those primitives and document its recipe before implementation. Never invent a parallel button, card, field, sheet, token, icon, type, radius, shadow, or interaction system. Custom visuals remain limited to WorkshopLM's work itself: Map geometry, evidence paths, generated media, charts, slide previews, image tiles, and storyboard frames.

All primary language is plain, professional, and action-first. Object names are `Workshop`, `Sources`, `Map`, `Brief`, `Style`, `Outputs`, `Storyboard`, and `Video`. Primary actions are `Add source`, `Record voice`, `Add transcript`, `Show source`, `Show on map`, `Approve brief`, `Choose style`, `Create outputs`, `Approve storyboard`, and `Create video`. Supporting actions may use `Back`, `Close`, `Edit`, `Save`, `Retry`, `Cancel`, and `Open`. States are limited to `Draft`, `Ready for review`, `Approved`, `In progress`, `Ready`, `Needs update`, and `Couldn't create`. Technical terms such as gate, contract, package, artifact, provenance, render, Visual DNA, model ID, version ID, `FRAME.md`, and `DESIGN.md` appear only in technical Details or exported files.

## Locked decisions

- Professional and team workflows only; Education/Learning mode is removed.
- One focused Capture → Shape → Deliver experience rather than separate work and education products.
- TypeScript monorepo using pnpm workspaces and Turborepo.
- The hackathon demo is local-first and does not require hosting, a separate WorkshopLM account system, Supabase, cloud object storage, or a remote worker.
- Local persistence uses SQLite in WAL mode for Workshop metadata/jobs and a workspace-owned filesystem directory for sources and generated artifacts.
- Original source files and normalized representations remain in the local Workshop data directory for the core demo. Hosted indexing may be added later, but source grounding does not depend on upload.
- The only required network dependency for the core demo is the OpenAI API; HyperFrames and output rendering run locally.
- WorkshopLM's local application is the complete user-facing product: Sources, text/Realtime Conversation, Map, Brief/Style, Outputs, Storyboard, Video, and source trace live inside one three-column workbench.
- Codex desktop/CLI remains the verified plugin host, orchestration doorway, development environment, and optional launch surface. It does not own the product's primary Conversation experience and is not required to remain visible while a professional uses WorkshopLM.
- Codex app-server brokers ChatGPT/Codex login and account state. WorkshopLM does not receive, expose, or persist raw ChatGPT auth tokens and does not create a second account system.
- WorkshopLM is packaged as a unified ChatGPT/Codex plugin: workflow skills plus a bundled local stdio MCP server and compact review widgets. The plugin is the native discovery/orchestration layer; the local browser workspace remains the full production interface.
- Core plugin use requires no connected app. Granola, Google Drive, and other source apps are optional adapters after the sanitized file/URL path works.
- One canonical semantic project graph is the source of truth.
- The whiteboard has two views over that graph:
  - **Map:** editable Excalidraw thinking surface.
  - **Sketch:** polished, regenerable hand-drawn output of an approved graph version.
- Sketch is not a second editor. Graph changes mark it stale until refreshed.
- The whiteboard becomes the executable brief.
- Source claims and citations remain linked through downstream artifacts.
- Style is layered into Brand Foundation, Intent Profile, and versioned Visual DNA.
- Storyboard approval is mandatory before HyperFrames video rendering.
- Image coherence uses locked references, evaluation, and selective regeneration; no unsupported perfect-consistency claim.
- The product creates its own truthful demo package and final meta-demo video.
- The primary demo scenario is meta: WorkshopLM creates the assets and final video used for its own Build Week submission.
- WorkshopLM is organized into Workshops; each Workshop is the durable top-level container equivalent to a Notebook in NotebookLM.
- A Workshop contains its sources, conversation, semantic map, approved brief, style, and outputs.
- Preserve NotebookLM's familiar Sources -> central Conversation/work -> Production mental model inside one WorkshopLM application. The stable three-column geography is a locked product decision, not a temporary desktop treatment.
- Adapt NotebookLM's interaction architecture to professional production without copying Google's chrome, marks, terminology, education framing, or exact dimensions. WorkshopLM continues to use the established OpenAI Figma-library design language and its own Map, approvals, Style, provenance, and version-history contract.
- Product language is plain, professional, and action-first. Prefer familiar nouns (`Sources`, `Map`, `Brief`, `Outputs`, `Storyboard`, `Video`) and short verb–noun actions (`Add source`, `Approve brief`, `Create outputs`, `Show source`, `Create video`). Internal nouns such as gate, contract, package, artifact, provenance, render, Visual DNA, and version identifiers stay out of primary labels unless the user opens technical Details.
- Deliver is the durable Outputs history over one shared grounded Workshop core, not a mandatory fixed package.
- `Production Kit` is rejected as the output label.
- Pipecat is deferred from the MVP; retain an adapter seam and revisit only for telephony, provider switching, or server-side audio pipelines.
- OpenAI project reasoning and structured operations use the Responses API with the GPT-5.6 routing policy: `gpt-5.6-sol` for quality-critical reasoning, `gpt-5.6-terra` for balanced structured work, and `gpt-5.6-luna` for repeatable high-volume work. Per-operation defaults may change only with recorded quality, latency, and cost evidence.
- Project grounding uses local parsing, deterministic chunks/locators, SQLite FTS5/BM25, exact text search, and standard plugin `search`/`fetch` tools. GPT-5.6 receives retrieved evidence bundles and may not mark a factual claim `verified` without durable claim→chunk→source edges. Hosted OpenAI `file_search` is an optional adapter/comparison, not a requirement.
- WorkshopLM's in-product Conversation is the primary capture path. Text uses the grounded Conversation composer; voice uses the existing narrow `gpt-realtime-2.1` WebRTC capture control inside that center-column experience. Standard API keys remain server-only, and no native ChatGPT thread/voice synchronization is required or claimed.
- Narration uses `gpt-4o-mini-tts`, stores panel-level provenance, and clearly discloses that the voice is AI-generated.
- Batch image generation uses the direct Image API with `gpt-image-2`; conversational image edits may use the Responses image-generation tool.
- `Notedex`, `Notex`, and `ChatGPT Notes` are rejected names.
- **Production** is the persistent user-facing production column. **Outputs** is the creation and history collection inside Production; an **Output type** is a guided creation path; an **Output** is one professional deliverable; an **Output set** is a user-selected group created from the same approved state.
- Exactly two blocking approval gates exist: approve the Map as the brief, then approve the storyboard before video. Style selection is inline review, not a third gate.
- The demo path includes a one-click sanitized sample Workshop with no private connector, plus a bounded live operator run on sample sources for recording and optional inspection.
- The public demo video is the primary judge experience and must prove the live thought-to-delivery seam clearly. The sanitized fixture makes recording repeatable and supports optional inspection without requiring judges to supply or spend their own API credits.
- The submission is a local plugin/developer-tool experience. Provide concise installation instructions and the verified supported platform, but do not divert implementation time into making judges recreate the full local stack. The verified Spike E surface controls the README, Devpost, and video platform claim.
- Workshop progress is represented by independent gate flags rather than one linear lifecycle: `transcript_ready`, `board_approved`, `brief_ready`, `style_locked`, `storyboard_approved`, and `video_rendered`. Staleness remains a per-object overlay.
- Five live integration spikes gate only the provider- or host-dependent implementation they inform: native ChatGPT task/account/voice synchronization (with Realtime fallback), local plugin search/fetch grounding (with optional File Search comparison), GPT Image 2 batching, local HyperFrames rendering, and unified-plugin loading across the available Work/Codex surfaces. Monorepo foundation, domain work, deterministic adapters, GUI shell, and fixture work proceed in parallel.
- The GUI is the flagship product proof. Plugin widgets remain compact doorways; they never duplicate the Map, Outputs, Storyboard editor, or asset browser.
- Freeze `packages/domain` contracts before parallel feature work. Afterward, the integrator may approve compatible changes with tests; only product-changing breaking decisions escalate.
- Freeze all provider-independent domain contracts by July 14 end of day. The transcript/turn schema may remain behind its adapter interface until Spike A resolves, but must freeze by July 15 morning without delaying independent lanes.
- Use at most four concurrent execution slots: one primary integrator and three isolated implementation lanes.
- Run a daily end-to-end acceptance path once the first vertical slice exists. A red run pauses breadth, not the build.
- Deterministic presentation and infographic rendering uses HTML/CSS templates exported to PDF/PNG.
- HyperFrames is the primary local deterministic video renderer. WorkshopLM uses no HyperFrames API, auth, remote job, polling, or download flow. A minimal local FFmpeg composition behind the same adapter is the fallback; do not add a parallel Remotion stack unless live evidence proves it necessary.
- Build a provenance surface showing how WorkshopLM and its submission were created, including `log.md`, commits, available Codex task evidence, and measured time-to-first-output.

## Engineering decisions delegated

The primary integrator selects and justifies these without separate founder approval unless a choice crosses an escalation boundary in `AGENTS.md`:

- monorepo package boundaries and canonical schemas;
- local database, artifact storage, and durable-job implementation within the locked SQLite/filesystem boundary;
- renderer implementation within the locked HTML/CSS and HyperFrames/FFmpeg boundaries;
- job retry, cancellation, timeout, and partial-success mechanics;
- cache, version, and stale-state implementation details;
- responsive breakpoints, accessible component behavior, and keyboard interactions;
- test organization, fixtures, mocks, and CI commands.

## Progress

### 1. Research and requirements

- [x] Verify Build Week rules, deadline, judging criteria, track, and submission requirements.
- [x] Research NotebookLM strengths and limitations.
- [x] Research Pomelli website-to-brand workflow.
- [x] Research Canva's editable multi-format boundary.
- [x] Research OpenAI Realtime, File Search, image generation, and tool architecture.
- [x] Verify and lock the official OpenAI API/model map for GPT-5.6, File Search, Realtime 2.1, GPT-4o mini TTS, and GPT Image 2.
- [x] Research Excalidraw, Google Flow coherence mechanics, and HyperFrames handoff.
- [x] Establish professional-only positioning and Work & Productivity track.
- [x] Select WorkshopLM as the working public name.
- [x] Map source-to-output workflows and evaluate ChatGPT Sites and Pipecat boundaries.
- [x] Inspect the live NotebookLM library, Sources, Chat, citation, Studio, and output-viewer flows in Chrome with privacy-safe screenshots.
- [x] Research the installed ChatGPT/Codex plugin landscape and lock the use-now, source-adapter, reference, fallback, and stretch boundaries in `research/plugins/README.md`.

### 2. Product design

- [x] Lock the flagship visual GUI contract in root `DESIGN.md`.
- [x] Approve the canonical semantic graph.
- [x] Approve Map and Sketch as two views over one graph.
- [x] Approve Workshop as the user-facing top-level container.
- [x] Approve system architecture and component boundaries.
- [x] Approve end-to-end UX and data flow.
- [x] Approve versioning, stale-state propagation, and approval behavior.
- [x] Approve failure, retry, cancellation, and partial-success behavior.
- [x] Approve responsive and accessibility behavior.
- [x] Approve verification strategy and judge-test path.
- [x] Approve the meta-demo scenario in which WorkshopLM creates its own submission assets and video.
- [x] Approve Outputs / Output type / Output as the replacement for the rejected fixed-package label.
- [x] Lock exactly two approval moments for the product and three-minute demo: brief and storyboard.
- [x] Audit the live MVP against NotebookLM and the official Apps in ChatGPT UI guidance; record the simplification direction in `research/ui-ux-simplification-audit-2026-07-14.md`.
- [x] Supersede the original persistent three-rail/six-tab MVP and the interim three-tab concept with the one-current-object shell. This was the verified July 14 simplification milestone and is now historically superseded by the July 15 locked three-column workbench decision below.
  - 2026-07-15: the header Workshop name now opens one official `SideSheet` that lists durable Workshops and creates a clean empty Workshop without adding navigation chrome. SQLite persists the active Workshop, every mutation and artifact path is scoped to its Workshop, background Video jobs render their queued Workshop without changing the one open in the UI, and plugin `search`/`fetch`/trace default to that same active boundary.
- [x] Finish and verify the July 14 one-current-object shell. Preserve its official primitive layer, accessibility work, canonical language, and regression coverage as inputs to the newly authorized three-column rebuild; do not treat its contextual-sheet composition as the final UX.
- [x] Rebuild WorkshopLM as one responsive three-column workbench with persistent/collapsible Sources on the left, WorkshopLM Conversation or the selected work object in the center, and persistent/collapsible Production progress on the right.
  - 2026-07-15 Phase 2: stable desktop and compact geography, in-product Conversation, and both rail controls are implemented. The Codex in-app browser verified 220px/252px open rails, independent 48px restore handles, an 808px-to-1184px canvas expansion at 1280px, and unchanged 390px Source-sheet/object-switcher behavior. Collapse state is intentionally session-local and reduced motion removes its transition.
- [x] Add WorkshopLM's own grounded text composer to the center column and integrate the existing Realtime voice capture there. Persist each completed turn under the existing transcript/Source contract; do not depend on Codex chat state for the user workflow.
  - 2026-07-15: local SQLite persists typed user/assistant turns, canonical `search` operations, exact claim/chunk/source/locator/snippet edges, and voice-origin turns. A text question remains a query and cannot silently become evidence; a saved voice thought explicitly becomes a private Source before the grounded reply. This proves the durable local contract and UI, not Responses streaming or live speech-to-speech behavior.
- [x] Make Capture -> Shape -> Deliver an interactive progress model with completed prerequisites, blockers, and one explicit next action; remove workflow-engine responsibility from the swapping header action.
- [x] Keep source selection, the claim under review, and its exact evidence excerpt visible together. Changing source scope must reveal affected downstream work before the user loses context.
  - 2026-07-15: a pending Source change now selects that Source, preserves its excerpt and locator, keeps the current canvas behind it, and names Map plus each existing downstream artifact before mutation. The disclosure explicitly says Style stays the same and offers only `Update sources` or `Cancel`. Desktop and 390px browser checks caught and repaired a duplicate rail/sheet disclosure; the accepted change then proved exact stale propagation while keeping Style ready.
- [x] Make Production an accumulating rail from first use through Video, with visible locked, empty, planned, in-progress, review, current, failed, and needs-update states plus bounded retry.
  - 2026-07-15 Phase 2: the rail now covers the complete lifecycle in plain professional language. Synchronous Presentation/Infographic failure persists with `Try outputs again`; one failed image becomes `Partly ready` and routes to selective review/replacement; a requested replacement holds the workflow until new work exists; queued Video exposes cancellation; and terminal Video failure appears only after two automatic attempts with `Try video again`. Browser proof and screenshots are recorded in `artifacts/ui/production-recovery-qa-2026-07-15.md`.
- [x] Document every implemented shell, rail, Conversation, and responsive pattern as an Apps-in-ChatGPT-Figma-derived WorkshopLM composite before implementation; add each to the component inventory, reusable UI layer, and conformance allowlist. Future collapse, splitter, and inspector patterns remain subject to the same rule before they ship.
- [x] Adapt the three-column model for mobile with a compact Chat / Map / Brief / Outputs / Story object switcher; preserve source count, production state, and the next action without squeezing desktop columns or hiding critical capability.
- [x] Verify the rebuilt Phase 2 path at 1200x800, 1024x768, 390x844, keyboard-only, 200% logical zoom, reduced motion, and WCAG AA. The 28-case production-browser suite covers Conversation, exact citation opening, embedded voice capture, stable rails, and the existing seam. Re-run this gate after collapse controls or live provider behavior land.
- [x] Move Style behind a compact Brief summary and an on-demand sheet; keep Sketch and Storyboard inside Outputs; keep source trace and technical data in contextual source/Details sheets.
- [x] Complete the visual Outputs gallery with real presentation, infographic, image-set, storyboard, and video previews.
  - 2026-07-15: persisted deck and infographic HTML render as real gallery previews; the production-route fixture now renders, serves, displays, opens, and responsively captures the completed local MP4. Image generation still has only a planned manifest, not provider image bytes; keep this item open.
  - 2026-07-15: all five deliverables use one official media-card grid instead of image/Storyboard carousels beneath the primary work. The newest Presentation now owns the full-width hero position; Infographic, Image set, Storyboard, and Video follow as supporting deliverables in professional workflow order. Image set opens a dedicated responsive six-up review surface with honest per-panel states, source access, and direct real-file links when bytes exist. Keep the item open until provider image bytes replace the planned panels in the captured gallery.
  - 2026-07-15: the six-up surface now replaces generic `Image 1–6` labels with Hero concept, Systems diagram, Evidence chain, Decision visual, Storyboard sequence, and Section art; each card exposes only its grounded idea, status, exact Source, and selective replacement. Desktop, compact, mobile, 200% zoom, and generated-image interactions passed the production-browser suite. Provider image bytes remain the only visual-gallery completion blocker.
  - 2026-07-16 completion proof: the authorized live run replaced all six planned panels with verified GPT Image 2 bytes; those real images now populate the six-up Image set, Storyboard cards, finished Outputs gallery, and provider-narrated Video. `outputs/workshoplm-current-ui/08-current-outputs.png`, `artifacts/live-review/gpt-image-2-contact-sheet.png`, and the inspected Video contact sheet prove the complete visual family. The older planned-image blocker is superseded.
- [x] Remove raw Markdown, artifact paths, internal IDs, version mechanics, absent trace stages, and gate implementation language from normal UI while preserving them under Details or export.
- [x] Make each screen expose no more than three labeled actions in the default viewport and exactly one visually dominant next action when progress is possible. Hide actions that do not apply to the current state.
- [x] Add frontend tests for progressive disclosure, contextual citations, two approval bars, visual output previews, and suppression of incomplete provenance rows.
- [x] Re-verify the final simplified UI at 1200×800, 1024×768, and 390×844; capture a replacement screenshot set for the demo storyboard.
- [x] Adopt the six NotebookLM legibility gains as implementation acceptance criteria: instant explanation, stable geography, tangible source scope, real output previews, calm first run, and one-click citations.
- [x] Capture a privacy-safe live UI gallery covering the reset and complete recorded fixtures, including Map, Sources, citation, Brief, Style, Outputs, Storyboard, focused Output, and mobile states.
- [x] Rebuild the July 14 judge path around the former split surface in which ChatGPT owned Conversation and the Workshop owned visual work. This verified milestone is now historically superseded: the three-column rebuild must bring Conversation into WorkshopLM while retaining the proven Map, trace, Brief, Style, Outputs, and Storyboard behavior.
- [x] Restore the judge path as a constrained, toolbar-free Excalidraw Map: fixed source geography, typed semantic nodes, visible evidence and graph edges, direct movement/resize/text editing, contextual claim/source inspection, and a mobile review outline over the same graph.
- [x] Replace trivial approval states with a Brief coverage receipt, live Style contract, Storyboard coverage/runtime summary, and visibly named approved versions.
- [x] Replace four equal creation actions with one `Create outputs` action and ensure each Output appears exactly once.
- [x] Remove the July 14 persistent stage spine, generic Library, generic Details, and ungrounded browser capture chrome. The new grounded WorkshopLM Conversation composer and operational stage model are separately reopened above and must use the established Figma-derived system.
- [x] Inventory the exact components, variables, styles, icons, and display-mode templates in `Apps in ChatGPT · OpenAI Official (Community)` from an editable Figma design-file copy; record names and source identifiers without inference.
- [x] Replace every approximated or custom WorkshopLM shell component and style with real reusable implementations of verified assets or documented compositions from the official Figma library. Permit custom rendering only for domain content such as Map geometry and generated artifact media. `data-oai-component` attributes and final-cascade CSS overrides do not satisfy this item.
- [x] Replace the string-presence UI test with conformance checks that cover every judge-visible element, the full CSS cascade, rendered computed styles, interaction states, responsive layouts, icons, and accessibility behavior.
- [x] Capture a fresh final screenshot set for the simplified Map, source trace, Brief/Style, Outputs, Storyboard, focused Output, and mobile review path.
- [x] Update frontend regression coverage for the judge path, including duplicate-artifact, one-dominant-action, hidden-unavailable-action, and no-raw-internal-language assertions. Full empty/loading/partial/error state coverage remains separately open below.
- [x] Re-verify keyboard focus order, WCAG AA contrast, reduced motion, and 200% zoom after the screen-composition rebuild.
  - 2026-07-14: the production UI passed at a real 600×800 Chrome viewport, the exact CSS-pixel reflow equivalent of 200% on a 1200px-wide browser, with no horizontal overflow. The automated browser boundary did not expose its native zoom-menu value, so no claim is made that the browser chrome itself was inspected at `200%`.

#### P0 official-component rebuild

Completion means implementation fidelity, not visual resemblance:

The reusable primitive foundation below is verified and remains complete. It does **not** prove that the current screen composition, information hierarchy, or language is finished; those gates are reopened in the next two sections.

- [x] Turn `packages/ui` into the real WorkshopLM UI layer with tested exports for `FullScreenShell`, `NavigationHeader`, `Button`, `IconButton`, `Token`, `Checkbox`, `Input`, `TextArea`, `Card`, `ListGroup`, `ListRow`, `EntityCard`, `Carousel`, and `CarouselRow`.
- [x] Implement each primitive from its recorded Figma node, variants, typography, spacing, state, radius, border, shadow, and icon source. Form controls must inherit the verified SF Pro web stack and match hover, pressed, focus, disabled, selected, checked, and error states.
- [x] Replace every ordinary HTML control and shell container in the judged path with the reusable UI layer. Domain interactions such as Map nodes, evidence edges, charts, slide media, image tiles, and storyboard imagery may stay custom, but their controls and containers may not.
- [x] Replace text glyphs and improvised symbols with the verified official iconography. Every IconButton must have an accessible name and visible tooltip where its meaning is not obvious.
- [x] When the library has no ready-made component, add a named WorkshopLM composite only after documenting its recipe in the Figma inventory. A composite may arrange official primitives and spacing tokens; it may not introduce independent chrome tokens, control geometry, typography, shadows, or interaction behavior.
- [x] Remove dead MVP shell CSS and isolate allowed domain rendering so no legacy selector can override official component states. The full application cascade must contain no retired structural color, radius, type, shadow, or spacing value outside explicitly documented generated-media exceptions.
- [x] Correct the known audit failures: 44px source Token versus the verified 42px reference, non-inheriting button fonts, legacy green locked-Style shadow, native checkbox appearance, glyph Back/Close icons, unmapped Brief/Storyboard actions and fields, the unmapped approval receipt, and missing current-object identity on mobile.
- [x] Add one inventory-backed exception manifest for custom domain rendering. Any custom judge-visible class absent from that manifest fails conformance.

#### P0 professional layout and language

Use NotebookLM's screenshots as behavioral evidence, then express the result in the official OpenAI system:

- [x] Keep one stable orientation frame: Workshop name and current object at top left, Sources in one consistent location, one focused center object, and one primary next action. Do not reintroduce persistent workflow tabs or a duplicate browser chat.
- [x] Make Sources feel as immediate as NotebookLM: one labeled count opens the source list; selection is directly visible; one click from any citation reveals the exact source excerpt and locator; closing it returns focus to the originating claim.
- [x] Make Outputs behave like NotebookLM's useful Studio history without copying its panel: each output is a recognizable named object with a real preview, type, freshness, source coverage, and one obvious open action. Creation controls do not compete with the output history.
- [x] Preserve the professional control advantage through progressive disclosure: Map editing, Brief approval, Style selection, Storyboard editing, and Storyboard approval appear only when relevant. The user sees two blocking approvals—Brief and Storyboard—not internal gate machinery.
- [x] Replace judge-visible copy using this canonical dictionary:

| Current or internal wording | Required user-facing wording |
| --- | --- |
| `Grounding this Workshop` | remove; the control reads `{n} sources` |
| `Evidence becomes structure` | `Map` or remove when the object title already says Map |
| `Approve as brief` / `Approve map as brief` | `Approve brief` |
| `Production contract` | `Brief` |
| `Visual contract` | `Style` |
| `Lock one coherent system` | `Choose a style` |
| `Lock Style v1` | `Use this style` |
| `Update Style v1` | `Update style` |
| `Coherent delivery package` | `Outputs` |
| `One system. Every format.` | a concrete output title or remove |
| `Generate package` / `Refresh package` | `Create outputs` / `Update outputs` |
| `Open package` | `View outputs` |
| `Trace` | `Show source` |
| `Illuminate path on Map` / `Highlight on Map` | `Show on map` |
| `Provider render pending` | `Images not created yet` |
| `Editable before the expensive step` | `Review before video` or remove |
| `Prepare render` | `Create video` |
| `Current` / `Stale` | `Up to date` / `Needs update` |
| `FRAME.md` / raw Markdown | formatted `Brief`; file name only under Details or export |
| `DESIGN.md` / `Visual DNA` | `Style`; technical name only under Details or export |
| `Brief approved` / `Style in use` repeated in cards | show `Approved` or `Current` once, only when it affects the next action |
| `Save` on an unchanged form | hide until the user changes something |
| `Update outputs` while outputs are current | hide; show only when an input `Needs update` |
| repeated `Open` and `Show source` buttons in the gallery | make the output card open; move source trace into the focused Output |

- [x] Audit every visible string. Labels identify the object; buttons state the result of clicking them; helper text explains only a consequence the user would not reasonably know. Use sentence case, avoid redundant subtitles and marketing copy, keep primary actions to two or three words, and never expose implementation terms in the default view.
- [x] Pass a five-second orientation test on Map, Brief, Outputs, Storyboard, Sources, and mobile: a first-time professional can answer `Where am I?`, `What is this based on?`, and `What should I do next?` without opening another surface.
  - Five independent cold review tasks passed the final screenshots after focused Outputs were corrected to preserve artifact-specific source scope and a visible title/type/version. Native PNG OCR and DOM assertions confirmed the full Storyboard identity and approval label when two review renderers initially corrupted those pixels.

#### P0 screen-by-screen simplification

Use the NotebookLM screenshots for orientation behavior and the official Apps in ChatGPT inventory for every piece of chrome. Preserve all implemented capability; reduce only simultaneous exposure.

- [x] **Shared frame:** use `FullScreenShell` + `NavigationHeader`; show Workshop, current object, one `{n} sources` control, and at most one primary action. Back appears only for a nested object. A compact Workshop/current-object identity is allowed; workflow breadcrumbs, stage navigation, duplicate page titles, and persistent side rails are not.
- [x] **Map:** let the source → claim → decision relationships own the canvas. Open one `ClaimInspector` only after selection. Show `Approve brief` when reviewable or `View brief` when approved; do not add a second approval card or repeated state block to the default canvas.
- [x] **Sources and evidence:** use `SourceSheet` and `EvidenceSheet` as temporary layers. Source selection stays visible and reversible. A citation opens the exact excerpt and locator in one click, then returns focus to the originating claim when closed.
- [x] **Brief and Style:** render the Brief as a readable executive document, never raw Markdown. Show Style as one compact summary row or card; `Choose style` opens the fields in a sheet. Do not keep a Style editor beside the Brief. Show `Create outputs` only after Brief and Style are ready.
- [x] **Outputs:** use media-first `EntityCard`/`Carousel` compositions. The card itself opens the Output; source trace lives inside the focused Output. Show `Create outputs` for an empty state, `Update outputs` only when inputs need update, and `View storyboard` only when a Storyboard exists. Do not repeat freshness or source counts when they do not change a decision.
- [x] **Storyboard:** use one `CarouselRow` filmstrip, one large selected panel, and one contextual editor. `Save changes` appears only when dirty and names its approval and Video consequences. `Approve storyboard` is the only dominant action before approval; `Create video` replaces it after approval. Secondary source trace opens on demand.
- [x] **Focused Output:** give the work maximum space. Keep Back/Close, title, and only the relevant edit/export/source actions. Technical Details stay collapsed.
- [x] **States:** design empty, loading, partial, error, ready, and needs-update states with the same official primitives. Each state names what happened and the single best next action; it never exposes provider or queue internals.

#### P0 component derivation rule

- [x] Before adding any judge-visible pattern that is absent from the official inventory, document a named composite in `research/openai-apps-figma-component-inventory-2026-07-14.md` with its exact official primitive recipe and Figma sources.
- [x] Implement that composite in `packages/ui`, add it to the machine-readable allowlist, test its states and responsive behavior, and only then use it in `apps/web`.
- [x] Do not create page-local buttons, inputs, sheets, cards, badges, pills, menus, tabs, icons, type styles, radii, shadows, spacing values, or interaction states. Custom rendering remains limited to Map geometry, evidence paths, charts, and generated media inside official containers.
- [x] Maintain a screen-to-component reconciliation table for Shared frame, Map, Sources, Evidence, Brief, Style, Outputs, Storyboard, and focused Output. Any unmapped judge-visible element blocks completion.

#### P0 proof before completion

- [x] Update structural tests so they fail for unmapped interactive elements, raw shell controls, glyph icons, unapproved tokens, page-local chrome, or custom chrome outside the exception manifest.
- [x] Re-run computed-style assertions for exact official geometry and typography in every interaction state used by the redesigned screens.
- [x] Replace screenshot baselines for reset and completed fixtures at 1200×800, 1024×768, and 390×844 across Map, Sources, Evidence, Brief/Style, Outputs, Storyboard, and focused Output.
- [x] Verify keyboard-only completion of both approval paths, focus return after sheets, visible focus, WCAG AA contrast, reduced motion, screen-reader names, 200% zoom, and no horizontal overflow.
- [x] Run a copy inventory test that snapshots every judge-visible label and rejects retired/internal vocabulary outside technical Details and exported files.
- [x] Complete a manual Figma-to-runtime reconciliation table with source node, code component, rendered measurement, state coverage, and screenshot evidence for the final compositions.
- [x] Conduct five independent first-time review passes: each reviewer must identify the current object, source scope, and next action within five seconds on every primary screen, with zero critical navigation failures.

### 3. Specification and implementation plan

- [x] Write the proposed design to `docs/superpowers/specs/`.
- [x] Self-review the specification for placeholders, contradictions, ambiguity, and excessive scope.
- [x] Reconcile and lock the written specification and execution architecture.
- [x] Write the dated execution runbook (`PLAN-2026-07-13.md`).
- [x] Write the task-level foundation and integration-spike plan under `docs/superpowers/plans/`.
- [x] Write subsequent task-level workstream plans as each reaches execution.

### 4. Integration spikes

- [ ] Capture and timestamp the original raw voice brainstorm before product code exists.
  - This can no longer be backfilled. Capture a dated contemporaneous founder-brainstorm recording for the demo reveal, label it honestly, and keep the original pre-code requirement open.
- [x] Verify API-key authentication and list access for the required OpenAI model IDs without spending generation credits; record any entitlement or spend-cap uncertainty.
- [x] Spike A: the app-server host probe remained credential-blocked through the July 14 deadline, proving native text/voice-turn persistence cannot be a dependency. The implemented capture-only `gpt-realtime-2.1` path is retained as the verified transport foundation and is now superseded as a product decision by WorkshopLM-owned browser Conversation and speech-to-speech collaboration.
- [x] Spike B: deterministic normalized local sources, standard plugin `search`/`fetch`, FTS5/BM25 retrieval, and durable Source/Claim citations; live GPT-5.6 expansion remains entitlement-gated.
  - 2026-07-15: replaced the JSON substring scorer with a durable SQLite FTS5 `unicode61` index over normalized chunk and linked claim text. Worker and plugin search now use BM25 ranking; exact fetch remains bound to `sourceId` + `chunkId`. The live and refreshed installed fixtures each contain three indexed sanitized chunks, and `editable production system` ranks `Design · Map` first.
- [x] Spike C: six-image batch manifest, locked reference, selective regeneration contract, and the 2026-07-16 live six-panel GPT Image 2 run.
- [x] Spike D: local HyperFrames CLI health check, validated three-scene composition, disclosed narration fixture, and rendered MP4.
- [x] Spike E: locally install the unified plugin, invoke its skill, call its stdio MCP tools, and record actual Work/Codex surface support.
  - Codex skill and tool proof is complete: an isolated plugin-only Codex profile installed enabled `workshoplm@workshoplm-local` version `0.1.2`, task `019f6474-bb34-7bf1-8ed0-65527e91b224` explicitly activated and read `$workshoplm`, then called `workshop_list → search → fetch` against the sanitized acceptance fixture. It fetched `Sanitized fixture · chunk 01` with one linked verified claim; installed manifest, skill, and server hashes matched the current worktree.
  - Fresh Codex desktop proof is also complete: task `019f5eb9-d996-7f42-ac5a-d4ed2cc8a324` activated the installed skill and called `workshop_list → search → fetch`, returning `chunk-seed-design`, verified claim `claim-seed-design-system`, and locator `Design · Map`. The root browser doorway returned HTTP 200 and rendered the current Map in the Codex in-app browser with no new console errors. The installed `0.1.2` package was refreshed and its tool hash matches the worktree.
  - 2026-07-15 current-manifest correction: version `0.1.3` replaces the obsolete local `widgets`/`dependencies` `.app.json` shape with the current unified `apps` map and registered optional Granola and Google Drive IDs. Dead compact-widget HTML was removed because the full GUI belongs in the loopback in-app browser. `codex plugin add` accepted and installed `0.1.3`; repository and installed manifest, app-contract, server, and skill hashes match. Fresh Codex task `019f67b9-177b-7250-a3cf-84d87774b14b` called `workshop_list → search → fetch` and returned one verified exact chunk from the sanitized fixture. The skill was omitted from that task only because Daniel's global installation exceeded the host's two-percent skill-context budget; prior isolated and desktop activation evidence remains valid because the skill bytes are unchanged.
  - 2026-07-15 host-boundary resolution: the installed `com.openai.codex` desktop app exposes Codex tasks, local plugin infrastructure, and the in-app browser; the separately installed `com.openai.chat` app exposes the legacy `Work with Apps` menu but no WorkshopLM or unified local-plugin surface. The designed fallback is final: Codex desktop/CLI plus the local in-app browser is the supported executable host, ChatGPT Work parity is not claimed, and no hosting is added merely to force parity. Installed `0.1.3` was reinstalled after a stale-cache regression, repository and cache hashes match, and a direct installed-server smoke returned the expected eleven tools plus exact grounded `search → fetch` evidence from the sanitized fixture. See `artifacts/spikes/plugin-supported-host-2026-07-15.json`.
- [x] Activate and verify the designed capture-only foundation for the unproven native task/voice synchronization spike; WorkshopLM-owned Realtime speech, grounded read tools, and interruption were subsequently live-proven on 2026-07-16.

### 5. Repository and platform foundation

- [x] Initialize Git and preserve dated Build Week commits.
- [x] Scaffold the pnpm/Turborepo monorepo.
- [x] Create `apps/web`, `apps/worker`, and focused shared packages.
- [x] Create the unified plugin manifest, WorkshopLM skill, local MCP server, and compact status/trace widget with tested persisted-fixture tools.
  - 2026-07-15: replaced summary-only plugin write acknowledgements with the real loopback Workshop API. Source import now normalizes and grounds the supplied URL, PDF, or bounded text file; Brief and Storyboard approvals reject stale requested versions; output tools persist real deck/image-plan/Storyboard state; render queues the real leased worker job; and trace resolves durable claim→chunk→source edges. An isolated no-provider run completed import → exact Map approval → deck/images/Storyboard → exact Storyboard approval → queued render with 59 indexed chunks and four deck evidence edges.
- [x] Establish linting, type checking, tests, deterministic local fixture reset, and fail-closed live-provider environment validation.
- [ ] Record every participating Codex `/feedback` Session ID, or the explicit reason it is unavailable, and designate the primary integrator session from actual build evidence.
- [x] Configure local SQLite WAL persistence boundaries, atomic filesystem artifact storage, and idempotent leased local jobs.
- [x] Freeze `packages/domain` v1 schemas, commands, gate flags, dependency edges, and `artifact.json` shape with tests.
- [x] Create a sanitized local demo fixture that requires no private connector and makes video capture deterministic.
- [x] Create a bounded live operator path on sample sources for recording the real demo seam.
  - 2026-07-15: `pnpm demo:live` passed its isolated no-spend preflight with the Brief approved, the Storyboard honestly `Ready for review`, two traced outputs, one planned GPT-5.6 request, six planned GPT Image 2 requests, five planned TTS requests, and zero provider calls. In live execution, all six image results now land before the operator crosses the Storyboard approval boundary; narration and video remain blocked until that point. Live execution requires `WORKSHOPLM_MAX_PAID_REQUESTS=12`; the separate nine-request routing benchmark requires its own ceiling. Paid execution remains unrun.
  - 2026-07-15 pre-spend narration gate: the zero-spend operator now validates all five narration inputs before any GPT-5.6, Image, or Speech request. It rejects missing text, invalid duration, inactive or tampered claim/source/chunk/locator edges, and any panel above the 4,096-character Speech limit. The current plan passed with five grounded panels, 507 total characters, a 112-character maximum panel, and zero issues; `paidCallsMade` remained false and verified Realtime voice remains the next required evidence.
- [x] Share and test Responses text extraction across the GPT-5.6 benchmark and live grounded-Map adapter so nested `output[].content[]` payloads cannot be mis-scored as empty.

### 6. Capture

- [x] Ingest local text/PDF files, safe public URLs, and sanitized meeting material.
- [x] Normalize local and connected-app/MCP sources for grounded `search`/`fetch` retrieval.
- [x] Upgrade and verify the current capture-only `gpt-realtime-2.1` path as WorkshopLM's center-column speech-to-speech voice agent: natural turn taking, barge-in, spoken responses, live transcript, and typed Workshop tool calls over the same durable Conversation/Source contract.
  - 2026-07-16 confirmed-write proof: an isolated live WebRTC turn used the exact current Workshop and Map version, requested Brief approval once, paused at the explicit-intent boundary, replayed the same provider call ID only after visible confirmation, persisted the successful mutation, and received a spoken provider continuation. Server-VAD now owns audio commits and rejected writes no longer trigger an automatic duplicate response. The founder's physical-microphone recording remains a separate demo-evidence item, not a missing product behavior.
  - 2026-07-16 live provider proof: controlled Chrome completed a real WebRTC conversation with `gpt-realtime-2.1` plus `gpt-realtime-whisper`, one grounded `search`, two exact `fetch` calls, spoken assistant output, a persisted private Source/turn, and exact item/event/call/response IDs. A second live session produced an actual cancelled provider response when the user barged in, then answered the redirect and persisted the interruption edge. Clean product screenshots and JSON evidence are under `artifacts/live-review/`. Keep this item open only for the founder's physical-microphone in-app-browser run and one provider-originated write that crosses the already-tested visible confirmation gate.
  - 2026-07-15 speech-agent implementation: the same Realtime control now has two explicit modes. Add Source retains transcription-only capture; center-column Conversation mints audio output with `marin`, automatic canonical tools, server-VAD response creation, and interruption, plays the remote WebRTC track, displays both transcripts, and persists the actual spoken assistant answer plus provider response/event IDs. Deterministic config, transcript, and durable-state tests pass. Keep this open for returning a confirmed write to the active channel and for a provider-backed microphone run.
  - 2026-07-15 confirmation continuation: after a professional confirms a pending Realtime write, the browser now returns the successful `function_call_output` to the same open data channel and requests the next spoken response. The output is consumed exactly once, including when confirmation lands while the channel is still connecting, and is discarded when the voice surface closes so it cannot leak into a later session. Keep this open only for provider-backed proof of the complete spoken loop.
  - 2026-07-15 provider-event pass: the browser recognizes the official completed-function event, sends it through the local adapter/executor, persists exact provider provenance, and returns the function output item plus continuation request over the Realtime data channel. Tool activity is visible in Conversation. Capture-only `tool_choice: none`, spoken output, interruption, explicit write confirmation, and provider-backed proof remain open.
  - 2026-07-15 confirmation pass: a Realtime Brief-approval call remained blocked and visibly asked for `Approve Brief`; only that button retried the exact provider call with explicit intent. The clean browser run proved `briefApproved: false → true`, call intents `[false, true]`, statuses `[failed, succeeded]`, no duplicate confirmation, and no visible internal tool name. Speech output, interruption, and provider-backed proof remain open.
  - 2026-07-15 executor foundation: Realtime and Responses now share one local executor contract with strict inputs, active-Workshop and Source-scope boundaries, exact version gates, explicit intent on writes, durable success/failure records, provider event identifiers, and idempotent retry. Keep this open until live Realtime events are bound, tool activity is visible in Conversation, spoken output and interruption work, and provider evidence is captured.
  - The server-minted ephemeral credential route, direct browser WebRTC session, capture-only VAD configuration, final transcript reducer, private Source persistence, and provider item/event provenance are implemented and live-proven for spoken response, interruption, safe read tools, durable transcript/tool provenance, and a visible provider-originated confirmed write. The founder microphone run remains separate authentic demo evidence.
  - 2026-07-15 final-run handoff: the no-spend live preflight now exposes `providerVoiceReady` and withholds its paid command until a verified WebRTC turn exists. After the turn is added in the local app, normal preflight preserves its exact transcript and provider item/event IDs through the clean rebuild; paid execution refuses before GPT-5.6, Image, or Speech dispatch if it is still absent.
- [x] Extract candidate goals, audience, claims, constraints, and unresolved questions from grounded claims with source locators.
- [x] Preserve claim-level evidence locators and source permissions.
- [x] Add WorkshopLM's grounded browser text Conversation using Responses streaming and durable Conversation state. Persist local turns, retrieval edges, tool calls, tool results, and OpenAI continuation identifiers without making hosted state the sole source of truth.
  - 2026-07-16 live provider proof: `gpt-5.6-terra` answered a real center-column question through Responses SSE after one grounded search and one exact fetch, persisted two successful provider tool calls, three evidence edges, final assistant text, and continuation ID `resp_0e7be4243393d77b006a588ca82dc481979809876c88b26147`. A first honest run showed that the previous three-request limit could end after retrieval without a final answer; four remains the hard ceiling and is now the product default.
  - 2026-07-15 streamed-loop implementation: the local browser now POSTs a bounded spend-gated Responses request, parses provider SSE, streams visible assistant text, executes canonical function calls, returns `function_call_output` with `previous_response_id`, and persists the final assistant turn, exact evidence, tool history, and continuation ID. Three deterministic fetch tests prove a direct answer, grounded search continuation, and fail-closed authorization. A clean browser run with live access disabled proved the route returned 503, the product used its grounded local fallback, and the saved answer exposed three source links without inventing provider continuation. Keep this open only for a provider-backed run through the same path.
  - 2026-07-15 provider-event pass: official completed Responses function items now execute locally, return exact `function_call_output`, persist successful or failed results and response/call/item IDs, and update the durable continuation ID. A simulated official event reached the real local API and rendered `Searched selected sources · Chat` with `Found 2 grounded evidence chunks` at 1200×800 and 390×844 without exposing raw tool or provider IDs. Keep this open until the app creates and streams the actual Responses request, persists the final assistant text/evidence, confirms writes, and captures provider proof.
  - 2026-07-15 shared-confirmation pass: the same failed-call → explicit-confirmation → one successful mutation contract is now available to Responses. Keep this open until the actual Responses stream invokes it and the final assistant text/evidence is durably persisted with provider proof.
  - 2026-07-15 local foundation: durable turns, BM25 retrieval, exact evidence edges, a typed canonical `search` operation, responsive Conversation UI, and a shared server executor that durably records tool inputs/results/provider IDs are implemented and verified. Keep this open until Responses events stream through that executor, tool activity renders in Conversation, continuation identifiers are persisted, and the path is provider-verified.
- [x] Expose one shared typed Workshop tool registry to text Responses and Realtime voice. Voice and text must obey the same source scope, approval, stale-state, privacy, and confirmation rules.
  - 2026-07-15: `packages/domain/workshop-tools.json` is the single validated contract for MCP, Responses, and Realtime. The same eight conversational tools reach both OpenAI adapters; every write requires explicit user intent and declares its visible effect. MCP executes the same contract, including source-scope invalidation, while its public protocol response strips internal policy metadata. The current Realtime capture session receives the official function schema with `tool_choice: none`, so this closes schema/policy parity—not live tool execution, tool-result persistence, provider continuation, or spoken behavior.

### 7. Shape

- [x] Generate the typed semantic graph from transcript/source ingestion with durable source metadata.
  - 2026-07-15: deterministic fallback ingestion now materializes up to twelve claim-level nodes per Source set with exact claim IDs, Source IDs, locators, concise titles, and stable spatial placement. `FRAME.md` ranks the strongest noncreative claim as the outcome and preserves up to three separate exact evidence statements.
- [x] Render and synchronize the Excalidraw Map, including persisted title, position, and size changes plus typed Undo.
- [x] Support typed, undoable user and AI graph operations.
- [x] Generate Sketch view from an approved graph version.
  - 2026-07-16 visible-output proof: replaced the hidden state-only Sketch with a deterministic Style-aware SVG artifact and one progressively disclosed Outputs card. The approved Map remains the only editor; Sketch retains semantic evidence color, graph edges, claim IDs, exact Source access, graph/style versions, SHA-256 provenance, and an explicit SVG finishing path. Map, Source-scope, and Style changes stale it until `Update sketch` or the standard `Update outputs` action regenerates a new version. Desktop and 390×844 production-browser screenshots and the complete recorded acceptance now prove the real path.
- [x] Produce and approve `FRAME.md` plus its executable representation.

### 8. Style

- [x] Create Brand Foundation from a public website URL.
  - 2026-07-15: replaced the immediate one-response hex scan with a two-step `Review style` flow that analyzes the page plus up to four public linked stylesheets, resolves logo/icon candidates, extracts exact color roles and font candidates, and persists only user-reviewed values. Redirects are revalidated against the private-network boundary before every fetch.
- [x] Support manual logos, exact hex values, licensed fonts, references, and negative rules.
- [x] Create professional Intent Profiles for client-facing pitch, board deck, and internal workshop use.
- [x] Expose website-first and manual-fallback Company Style setup in one progressive-disclosure sheet. The Workshop outcome selects and persists the matching Intent Profile; Style displays it as context rather than asking for the same choice again.
- [x] Create, preview, approve, and version Visual DNA.
- [x] Produce inspectable `DESIGN.md` plus machine-readable tokens.
- [x] Save and reuse Style Library snapshots across Workshops without coupling their applied versions. A new Workshop shows saved Styles first and hides website/manual setup behind `Create another style`; applying a different saved Style marks dependent work `Needs update`.

### 9. Deliver

- [x] Generate an asset plan from the approved graph, brief, evidence, Style Foundation, and Visual DNA versions.
- [x] Generate a source-traceable presentation.
- [ ] Raise the presentation from a deterministic proof artifact to the professional send-it bar: varied layouts chosen by content type, strong typographic hierarchy, exact Style tokens, unobtrusive citations, and an editable PowerPoint-world export. Verify it against real external content and a cold visual review; passing generation tests alone is insufficient.
  - 2026-07-16 content-aware provider pass: the actual provider-backed deck exposed two failures that passing template tests had missed—a clipped spoken headline and an under-informative process slide. Presentation Version 6 now turns `should show how … becomes …` speech into a complete claim and support line, detects explicit three-stage flows, and renders an editable process composition with quiet source trace. The approved image dependency, Style tokens, immutable history, and PowerPoint notes remain intact. The `.pptx` archive, five-page LibreOffice PDF, extracted text, five review frames, accepted contact sheet, and exact hashes are preserved in `artifacts/live-review/presentation-v6/`; external `Send`/`Revise` evidence remains the only closure gate.
  - 2026-07-16 editable-sequence refinement: the process layout now renders stage label plus supporting definition rather than label-only cards. The supporting language is deliberately semantic—gather, organize, create—so it improves comprehension without adding an unsupported factual claim. The accepted PowerPoint, PDF, and full-resolution frame are under `artifacts/live-review/presentation-sequence-detail/`; external `Send`/`Revise` evidence remains the closure gate.
  - 2026-07-16 provider-backed cover pass: generated media is now a versioned Presentation dependency rather than a separate gallery artifact. The renderer verifies the selected hero bytes and SHA-256, embeds the same image in HTML and editable PowerPoint, exposes provenance in the preview, and stales dependent decks on image revision. The accepted Version 4 review packet includes the editable `.pptx`, five-page PDF, individual slides, contact sheet, and live product screen. External audience judgment remains the final send-it gate.
  - 2026-07-15: the first production-quality renderer pass is implemented. HTML previews now use cover, statement, split, evidence, and recommendation layouts; every slide carries a quiet source line; exact palette and licensed-font tokens flow from Style; and each deck also produces an editable `.pptx` with claim/source notes. The in-app browser preview and a LibreOffice round-trip of the five-slide acceptance deck were visually inspected without overflow, archive errors, or console errors. Keep this item open until a genuinely external professional deliverable is produced and cold-reviewed at the full send-it bar.
  - 2026-07-15 dogfood zero: three real WorkshopLM strategy and research documents initially produced source-order headings, raw Markdown, and file paths. The deck planner now rejects metadata/table fragments, selects statement → risk → proof → recommendation evidence, derives concise headings, and uses professional source labels on-slide while retaining exact locators in HTML data and PowerPoint notes. The repaired five-slide `.pptx` passed archive, LibreOffice, and visual inspection. This is internal dogfood, not the required external audience or cold send-or-revise review.
  - 2026-07-15 dogfood one candidate: WorkshopLM ingested the shareable AI Collective chapter-startup FAQ plus the current public newsletter and produced a five-slide organizer brief with the official logo, exact palette, current 180+ chapter proof, concise citations, responsive HTML, and an editable `.pptx`. The first run exposed 5,179 script-derived web claims, PDF hard-wrap fragments, irrelevant proof selection, ignored logos, internal cover language, and duplicated headings; all were repaired in the product. The final PowerPoint passed archive validation, a five-page LibreOffice round trip, and visual inspection. The package in `outputs/dogfood-ai-collective-chapter-brief/` is ready for cold review. Keep this item open until a real professional returns `Send` or names the first required revision.
  - 2026-07-15 current-renderer refresh: the external-use candidate now rebuilds from a checked-in grounded brief and authorized logo with `pnpm dogfood:deck:build`. The current renderer removed the vague support fragment rather than padding it, produced fresh HTML, editable PowerPoint, five-page PDF, individual review images, and a cold-review contact sheet, and passed archive, page-size, and visual inspection. This closes reproducibility and stale-render risk; the human `Send`/`Revise` gate remains open.
  - 2026-07-15 evidence-layout pass: numeric proof now becomes an editorial metric composition in both HTML and editable PowerPoint instead of a decorative source box. The external deck visibly presents `180+ chapters` and `40+ countries` with the `400+ volunteer organizers` support beneath it. Slide footers use quiet source names while exact chunk locators and claim IDs remain in HTML metadata and PowerPoint notes. The rebuild command now regenerates and hashes the contact sheet too. The refreshed five-page PowerPoint/PDF passed visual inspection and all checks; external `Send`/`Revise` evidence is still required.
  - 2026-07-15 three-metric refinement: evidence layouts now promote as many as three source-backed headline measures instead of forcing the third into body copy. The external nine-slide deck shows `180+ chapters`, `40+ countries`, and `400+ organizers` as a balanced scan-first composition in both HTML and editable PowerPoint; its supporting sentence and exact trace remain intact. The intended-audience review remains the gate.
  - 2026-07-15 populated-split refinement: non-sparse split slides no longer reserve a colored rail for an oversized decorative index. The shared renderer now uses the full canvas for a claim/context split with one quiet Accent divider while preserving the deliberate sparse editorial composition. The external deck's event-format and sustainability slides passed full-resolution PowerPoint/PDF review.
  - 2026-07-15 sparse-claim quality pass: the external candidate's source-backed commitment had no separate supporting sentence and still rendered as an underfilled pale split template dominated by a decorative index. Sparse split claims now become an intentional dark editorial composition in both responsive HTML and editable PowerPoint, with an Accent rule, strong headline hierarchy, quiet visible citation, and the page index as a subordinate anchor. The refreshed five-slide PowerPoint archive, LibreOffice/PDF round trip, extracted text, individual review frames, and hashes passed inspection. This improves the candidate without inventing unsupported copy; the external `Send`/`Revise` gate remains open.
  - 2026-07-16 qualitative-proof pass: non-numeric evidence with explicit items now becomes a scannable commitment composition rather than a dark card that merely repeats the source name. The external trust slide presents conduct and privacy as two editable operating standards alongside a concise synthesis, while retaining its exact policy citations in notes. The current PDF/PPTX/contact sheet and review ZIP passed inspection; only a real intended-audience decision can close this gate.
  - 2026-07-15 decision-utility pass: internal review rejected the five-slide external candidate because professional polish without a plan or decision did not clear the send-it bar. The rebuilt nine-slide version uses new editable plan and decision layouts, adds policy and operating evidence, and labels every derived plan or recommendation as requiring HQ validation. Its reproducible source manifest, HTML, editable PowerPoint, nine-page PDF, previews, contact sheet, and evidence record are checked in. Internal visual and round-trip checks pass; intended-audience review is still required before this goal can close.
  - 2026-07-15 executive-cover pass: the actual nine-slide review exposed that the renderer silently repeated slide one's supporting sentence on the cover. Presentations now carry an intentional executive summary; generated Workshops use a factual Intent-aware line grounded in the selected Source count, while reviewable external briefs may supply a tailored framing line. The rebuilt HTML, editable PowerPoint, nine-page PDF, cover frame, contact sheet, and all three Intent Profile comparisons show the distinct cover without changing source-backed slide content. The external `Send`/`Revise` judgment remains open.
- [x] Generate a source-traceable infographic.
  - 2026-07-16 content-aware provider pass: the actual authorized-sample Infographic now turns explicit process evidence into a three-stage visual sequence in both responsive HTML and editable PowerPoint. The opening spoken transformation is complete, visible citations are quiet and human-readable, exact locators remain in the trace, and focused Version history preserves Versions 1–3 without replacing them. The accepted Version 4 packet includes HTML, editable `.pptx`, one-page LibreOffice PDF, text extraction, full-resolution review frame, hashes, and live desktop/compact screenshots under `artifacts/live-review/infographic-v4/`. Internal acceptance does not substitute for intended-audience review.
- [x] Create a source-traceable Audio Overview as a first-class versioned Production Output: structured grounded script, editable review, `gpt-4o-mini-tts` speech generation, streaming/local playback, standard download, duration, source coverage, model/voice metadata, file hash, and explicit AI-voice disclosure.
  - First implementation is one coherent professional briefing with concise executive, deeper overview, and decision-review postures. A two-host conversational format is a later quality expansion, not a prerequisite.
  - 2026-07-15 implementation and local proof: approved Brief/Style now create a three-part grounded script with exact claim/source/chunk/locator edges; edits create an immutable new version while retaining those edges; Map, Source scope, and Style changes mark prior versions `Needs update`. Audio Overview appears in the Outputs gallery and Create rail, opens a focused official-component review surface, reveals the exact Source per section, exposes AI-voice disclosure, and provides local playback plus standard download with duration, byte count, SHA-256, model, voice, instructions, and request ID provenance. The shared Responses/Realtime/plugin tool registry can create and trace it. The spend-gated Speech adapter validates a real WAV before persistence and retains the reviewed script on provider failure. The authorized Cedar response was generated, audibly inspected, and is now replayed byte-for-byte in the sanitized judge Workshop through a fail-closed source-and-hash gate.
  - 2026-07-16 live Cedar proof: the focused UI created and downloaded a 35.7-second, 1,713,644-byte WAV with request ID `req_65133c0f98074ce6a7dfff9220fc2e50`; persisted and downloaded SHA-256 both equal `4929d08428849a07771f6264836389c1fb126ce75e80b99fa8a40d099f843a96`. Local Whisper transcription recovered the intended executive finding, fragmented-workflow evidence, and exact-locator decision point. The product surface says `Cedar voice · AI-generated voice`; model metadata remains available in provenance rather than crowding the player.
  - 2026-07-15 send-it layout pass: the editable infographic moved from a narrow four-row table to a balanced 16:9 two-by-two narrative canvas. Responsive HTML uses the same hierarchy and collapses cleanly on mobile. Quiet visible source names, exact HTML locators, and PowerPoint claim/locator notes survived the round trip; the checked-in evidence image was visually inspected. This remains a supporting output behind the hero presentation.
- [x] Generate and evaluate a coherent GPT Image 2 batch.
  - 2026-07-15: the production batch now creates a 512×512 style-derived reference board, hashes and versions it with the approved Visual DNA, sends the same bytes and locked direction through `/v1/images/edits` for all six panels and selective retries, and refuses provider dispatch if the reference or sibling contract drifts. The July 16 live run generated all six real panels; full-set visual inspection accepted their shared art direction and professional visual jobs without regeneration.
  - 2026-07-15 provider-media gate: decoded Image API output must be a structurally complete PNG with a valid IHDR, positive dimensions, terminal IEND, no trailing bytes, and exact requested dimensions before a panel can become `generated`. Malformed, truncated, or wrong-sized bytes become a selective-retry failure and never enter Storyboard or Video provenance.
  - 2026-07-15 replacement-integrity gate: selecting one image for replacement increments only that panel, marks its bound Storyboard panel stale, and revokes the global approval. Recording the finished replacement rebinds the exact new image version and clears panel staleness while deliberately leaving the global Storyboard approval false for professional review.
  - 2026-07-16 immutable-image gate: a replacement request archives the current generated panel before incrementing it, preserves the last good visual while the new version is pending, and exposes exact earlier versions inside the existing replacement sheet. Default artifact resolution advances only when the new provider result is recorded; `image-panel-N-vM` routes keep every archived path independently reopenable.
- [x] Generate an editable, panel-level storyboard.
  - 2026-07-15: every generated Storyboard panel stores durable claim/source/chunk/locator evidence plus its bound image-panel ID and version. `Show source` opens that panel's exact Source; the focused review and HyperFrames composition resolve the same generated image bytes, and any version-changing image regeneration revokes Storyboard approval before narration or video can proceed.
  - 2026-07-16 immutable-history gate: every approved Storyboard remains identifiable and reopenable after a panel edit or image replacement. New content increments the Storyboard version and revokes approval; history retains the prior title, narration, duration, evidence, approval record, image-panel version, exact relative path, and SHA-256. The read-only official-component sheet serves the historical image through a version-specific route and exposes no save action.
  - 2026-07-16 timing-control gate: panel duration is editable as a plain `Seconds` field in the existing focused Storyboard. Only whole values from 1 to 120 may persist; a valid change creates a new Storyboard version, keeps the former approved timing in history, revokes approval, stales narration and every prior Video, and makes approval the next action before rendering.
- [x] Block video enqueueing until the current storyboard approval is persisted; the worker accepts current provider narration when present and otherwise uses the disclosed deterministic fallback.
  - 2026-07-15 render-integrity gate: HyperFrames staging now accepts provider narration only when it contains one unique clip for every approved Storyboard panel, maps clips into approved Storyboard order regardless of storage order, and verifies the stored SHA-256 of every narration clip and approved image immediately before rendering. Missing, duplicated, corrupted, or modified current media fails closed before HyperFrames runs; deterministic fallback remains available only when provider narration is genuinely absent or stale.
  - 2026-07-15 narration-fit gate: Speech responses must be structurally playable RIFF/WAVE audio with a valid byte rate and non-empty data chunk. A generated clip longer than its approved Storyboard panel by more than 250 ms is retained only as a retryable provider failure, never staged into a clipped final Video.
- [x] Render the sanitized approved storyboard through the local HyperFrames worker path with disclosed fixture narration and verified MP4 streams.
  - 2026-07-15: every render now emits `workshoplm-demo.provenance.json` with the MP4 hash and per-scene claim, chunk, source, locator, excerpt, image version/hash, and narration hash/provider. The traced submission Output set includes and verifies that sidecar as technical evidence.
  - 2026-07-15: Video is now a versioned persisted artifact instead of a fixed-file state flag. Each render writes immutable version-specific MP4 and provenance paths, stores Storyboard/Style/Visual DNA/image-batch inputs plus claim and content hashes, preserves all older files, and makes only the latest current version the canonical `video` alias. The Outputs grid shows history only when it exists; the focused player opens the selected historical bytes.
  - 2026-07-16 motion proof: the current provider-narrated Video Version 4 renders the approved five-panel Storyboard through a repository-local GSAP runtime rather than a CDN or static composition. The 25.00-second 1920×1080 H.264/AAC artifact has SHA-256 `bc3723f7cdc39fe0f08824b33d8a59956ccb018eb1c44290518ef19e8a120212`; HyperFrames reports zero lint or layout errors and 25/25 contrast passes. The five shorter Cedar clips intentionally leave breathing room inside their approved five-second scene slots and remain unclipped. No provider request was added.
- [x] Propagate upstream Map and Style changes into accurate downstream stale states.
  - 2026-07-15: source scope, Map, Style, Storyboard, bound-image, and narration changes now mark every prior Video version `Needs update` without deleting or redirecting it. Submission packaging refuses any Video that does not match the current approved Storyboard, Style, and image batch.
- [x] Preserve the professional weekly return visit: a new meeting expands the Map, stales the approved Brief and current downstream work, preserves the reused Style, and regenerates a new deck version with exact trace to the added Source. Historical versions remain visible without keeping the current Output set in a false stale state.

### 10. Meta-demo, provenance, and submission

- [x] Build the “How this was built” provenance surface from dated build evidence.
  - 2026-07-16 judge-proof pass: rebuilt the generated record around the complete connected Output set rather than only Presentation and Infographic. The Style-derived HTML now tells the raw-thinking → two-sign-off → finished-work story, lists exact formats, versions, hashes, and source-linked claim counts, links to versioned JSON, sorts real milestones chronologically, and keeps commit/task evidence collapsed. `Show original` promotes this record above the Output list without adding a tab or new navigation surface. Desktop/mobile inspection and the isolated 30-case production-browser suite passed.
- [x] Instrument and display time from first transcript segment to first rendered output.
- [ ] Produce the Devpost description, README narrative, deck, Audio Overview, thumbnails, storyboard, narration, and video as one traced WorkshopLM Output set. The recorded fixture now builds and verifies an 18-asset `partial` Output set with editable presentation and infographic files, a grounded Audio Overview script, source locators, file hashes, an input fingerprint, the rendered Video's per-scene provenance sidecar, and immutable HTML/JSON build trace; close this only after the provider-backed media and final submission video replace the explicitly recorded fallbacks.
  - 2026-07-15 claim reconciliation: removed the unsupported `WorkshopLM built itself` shortcut and separated the two proven layers: Codex built WorkshopLM from the goal file; WorkshopLM produced a traced 18-asset partial fixture. The demo gate now permits `final self-produced submission` only when the final copy, deck, Audio Overview, thumbnail, Storyboard, narration, and public Video verify as one non-partial Output set. The current manifest remains honestly `partial`, validates without stale or tampered assets, and records zero provider images, GPT-5.6 product reasoning, live voice, Video narration, or Audio Overview speech.
  - 2026-07-15 readiness integrity: `ready` now requires complete verified WebRTC, GPT-5.6, six-panel GPT Image 2, and panel-complete OpenAI narration evidence. Packaging recomputes the rendered MP4, image, and narration hashes from disk and semantically rebuilds the expected per-scene Video provenance; changed media or a changed provenance sidecar fails before a manifest can be emitted.
  - 2026-07-16 thumbnail send-it pass: replaced the fixture's one-second Video frame grab with a quiet 1280×720 WorkshopLM title composition using the locked `From rough thought to finished work` promise and the inspected provider-backed Outputs surface. The renderer binds its source Video, film plan, product screenshot, dimensions, and final PNG by hash. The final founder-derived Output set and public upload remain open.
  - 2026-07-16 Output-set thumbnail integration: the self-produced package no longer bypasses the designed cover with three automatic frame grabs. Its opening thumbnail now inherits the approved Workshop Style and evidence counts and uses the first hash-verified current generated image. The deterministic acceptance fixture now exercises that real-image path with a sanitized GPT Image 2 file; process and result thumbnails remain Video-derived. The 18-asset partial fixture verifies valid, current, and untampered; final founder media remains open.
  - 2026-07-16 generated-copy send-it pass: replaced the package's technically accurate but generic Devpost and README text with concise professional narratives generated from the current Workshop state. Provider counts, Source/claim totals, elapsed state, package status, and limitations remain dynamic; the product promise, professional problem, six-step path, two approvals, trace/edit/version differentiators, Codex role, HyperFrames role, and judge command are now explicit. The final founder-derived ready package and public form remain open.
  - 2026-07-16 provider-audio fixture pass: the current partial package now contains 25 hash-verified assets, including the exact 35.7-second provider-generated Cedar Audio Overview rather than script-only evidence. The fixture replays those tracked bytes with zero paid calls and still labels its deterministic Map and Video narration as fallbacks. Final founder media remains open.
- [ ] Include real UI evidence and the raw-transcript reveal. The production UI now implements and captures the responsive `Show original` before/after surface with fixture evidence. On July 16, `pnpm demo:capture-draft` recorded the complete reveal in a reviewable 58.0-second fixture walkthrough with the hash-bound provider image set and focused Sketch; close only after the dated contemporaneous founder brainstorm and final recorded footage replace that fixture.
- [ ] Make the public video the canonical judge path: show the live seam, one Codex plugin doorway moment, both approval gates, editable control, provenance, and the raw brainstorm → finished submission reveal in under three minutes. The repeatable capture now proves the two gates, semantic edit, source trace, local render, and reveal as one 12-beat sequence. The evidence-gated edit targets 2:20 across ten shots and fail-closes on every missing provider, founder, host, Codex, or final-export input; it intentionally remains a draft until those artifacts exist.
  - 2026-07-16 semantic finality gate: the film verifier still refuses to treat the partial acceptance manifest as evidence for the `WorkshopLM produced the final submission` reveal. It now also validates the sanitized provider run's exact Terra/Image 2/Cedar models, request provenance, panel counts, founder-Source limitation, copied Video hash, and real media streams instead of accepting file existence. Eight shots are ready; founder capture and the final meta-reveal remain blocked with four missing or unsatisfied evidence items including the final export. `/feedback` remains a separate Devpost requirement, not footage evidence.
  - 2026-07-16 recording-loop proof: ten bounded `gpt-4o-mini-tts` Cedar requests produced a hash-bound editorial narration set. The first 2:42 assembly exposed 4–6 second dead holds; the same story was retimed to 2:20 at 129.4 script words per minute, preserving natural Cedar cadence. Whisper recovered every intended sentence and `silencedetect` found no gap longer than 1.81 seconds. The refreshed H.264/AAC rough cut includes the inspected six-image live Outputs shot and the actual provider-narrated HyperFrames Video. It remains explicitly labeled with honest pending-founder/meta states, not the public demo.
  - 2026-07-16 founder-handoff proof: the final operator now accepts only a paired recording and transcript, requires a regular media file with both video and audio streams and at least three seconds of duration, bounds and normalizes transcript text, stages hash-bound evidence in an isolated root, and imports the transcript as `manual_import` with private Source permission. Missing streams, short text, incomplete flag pairs, roots outside `.workshoplm`, and inputs inside the reset target fail closed. This removes routine wiring from tomorrow's recording while leaving the founder and final-export gates honestly open.
  - 2026-07-16 final-package bridge: the final operator now creates the grounded Audio Overview script during preparation, reserves one additional Cedar request for its playable WAV, renders the approved Video, builds and verifies the submission Output set, and records its status, limitations, assets, and integrity result in the operator evidence. The film plan points to this final-operator manifest. No deterministic acceptance manifest can satisfy the meta-reveal gate anymore.
  - 2026-07-16 deterministic final edit: `pnpm demo:film:final` preflights every non-export evidence item, re-verifies the complete submission package against its Workshop state, substitutes the founder recording, generates the transcript-to-submission reveal from hash-bound evidence, uses the existing Cedar narration, and transactionally promotes the film only after final verification. Its clean caption system and meta-reveal layout were rendered and visually inspected at 1280×720. The current no-evidence run failed closed, preserved the draft plan byte-for-byte, and emitted no final MP4.
  - 2026-07-16 current-workbench recapture: the automated 12-beat fixture recorder was reconciled to the capture-first onboarding, persistent desktop Sources rail, current Style sheet, hash-bound GPT Image 2 replay, exact object navigation, queued-render status, source-truth language, focused Sketch, and connected Output-set reveal. The successful 58.0-second VP8 capture reached `briefApproved`, `storyboardApproved`, and `videoState: rendered`; its rebuilt 2:20 H.264/AAC rough cut uses all ten verified `gpt-4o-mini-tts` Cedar clips. Visual inspection accepted the refreshed contact sheet, focused Sketch, image-backed Presentation and Storyboard, and reveal frame. The film verifier still reports exactly the two honest blocked shots: founder capture and final meta-reveal.
  - 2026-07-16 visible-build-record pass: the final meta-reveal no longer asks narration alone to carry the provenance claim. It reads the hash-verified final submission manifest and build trace, then renders the exact Output, source-linked claim, sign-off, and hashed-asset counts over the finished-work thumbnails. `build-record` is now a required judge moment. The acceptance preview visibly proves the layout while final mode remains blocked on the founder package and derives its own numbers at render time.
  - 2026-07-16 sample-editorial-film pass: the clean final treatment is now reviewable as a complete 2:20 Cedar-narrated movie rather than ten still previews. Its generated meta-reveal binds the authorized sample transcript, acceptance manifest, build trace, and visible `7 Outputs · 6 source-linked claims · 2 sign-offs · 25 hashed assets` proof by hash. The sample manifest retains the two founder/final-package blocks and states plainly that it is not founder footage or the public submission. Final mode still requires the hash-bound founder transcript and ready founder-derived package.
  - 2026-07-16 Codex-doorway clarity pass: review of the actual clean movie found that the privacy-safe host footage foregrounded the Workshop more than the transition from conversation. A compact `CODEX → WORKSHOPLM · Conversation opens the visual workbench` cue now sits over that existing real footage in clean sample and final modes. It adds no fake host interface, no new claim, and no extra shot; the sample verifier requires the cue in the shot manifest.
  - 2026-07-16 submission-truth reconciliation: the Devpost draft, demo script, claim ledger, evidence audit, and checklist now promote the inspected Terra/Image 2/Cedar/Realtime evidence while keeping the authorized-sample boundary explicit. The demo script is retimed to the actual 0:00–2:20 shot plan and names the founder capture and final Output set as the only film-content gates. A new manifest-backed packet verifier passes with Terra, six images, five product narration clips, ten editorial clips, eight ready shots, two blocked shots, and exactly four unresolved founder/final-package brackets.
  - 2026-07-16 motion-floor reconciliation: the public-film compositor no longer permits long narration-backed screenshots to remain visually dead. Its verified push-and-drift treatment keeps proof readable while reducing the rough cut's longest static interval to the 3.03-second tail of the placeholder that will be replaced by the founder recording. This improves the draft without pretending the two blocked shots or public export are complete.
- [ ] Keep all judge-facing claims proportional to captured evidence. The current Devpost packet, demo script, evidence audit, and `submission/CLAIM-LEDGER.md` are reconciled to the July 15 proof floor; the packet leads with the professional problem and maintains explicit live/fallback publication slots. Close only after the final video, Devpost entry, screenshots, release tag, and submitted links pass the same gate.
- [ ] Produce a public YouTube demo under three minutes with clear audio.
- [ ] Complete repository, README, Codex collaboration, GPT-5.6, judge-access, and Devpost materials.
  - 2026-07-16 README and clean-clone reconciliation: the public entry point leads with the consultant/strategist/enablement persona, the source-defensible Presentation wedge, the two consequential approvals, and the weekly professional path. It accurately separates the credential-free fixture from the authorized Terra/Image 2/Cedar/Realtime sample run, documents plugin `0.1.3`, and keeps ChatGPT Work and the founder-derived final package unclaimed. A new public clone of commit `4cd938d` passed `pnpm install --frozen-lockfile`, `pnpm check`, `pnpm demo:reset`, `pnpm demo:e2e`, `pnpm demo:render`, `pnpm demo:thumbnail`, and live `/` plus `/api/workshop` inspection. Keep this item open only for the public Video URL, `/feedback` Session ID, final Devpost fields, release tag, and submitted-link verification.
  - 2026-07-16 one-command judge path: `pnpm judge:start` now runs the recorded acceptance before serving its exact data root. A live port-3107 smoke run returned HTTP 200 and the expected fixture state; `pnpm submission:packet:verify`, `pnpm check`, and `git diff --check` passed. The official rules were refreshed live, the repository MIT license was verified, and judge instructions now use a frozen install plus this command instead of opening an empty default workspace. Keep this item open for the same external submission gates above.
  - 2026-07-16 current-public-main proof: a fresh shallow clone of pushed commit `3d7221b` passed the frozen install, `pnpm check`, reset, recorded acceptance, second local render, thumbnail generation, packet verification, and `pnpm judge:start`. The live page and persisted Workshop API returned HTTP 200 on port 3001 with the approved Brief and Storyboard, rendered Video, one Source, Presentation, and Infographic. No provider request ran. Keep this item open for the public Video URL, `/feedback`, final Devpost fields, release tag, and submitted-link verification.

### 11. Daily acceptance and completion verification

- [x] Add a recorded-fixture mode to `pnpm demo:e2e` for repeatable demo diagnosis, with live-provider checks kept separate.
- [ ] Maintain and log the end-to-end acceptance path daily once the first vertical slice exists.
  - 2026-07-15 live-browser repair: the acceptance builder now completes onboarding and dismisses tutorial cues, and `pnpm demo:serve` opens the exact verified `.workshoplm/acceptance` data root instead of an unrelated fresh workspace. A Codex in-app-browser reload exposed and then verified the repair for a more serious seam: Excalidraw's mount-time shape normalization no longer posts false Map edits or revokes the approved Brief, Storyboard, rendered Video, and current Outputs. Real position, size, and label deltas remain persistable under focused regression tests. Keep this daily gate open until submission.
- [x] Verify the full Capture → Shape → Deliver flow in the live application.
- [x] Verify realistic desktop, tablet, and mobile behavior.
  - 2026-07-15 focused-output regression proof: the claim-level source trail initially made an older broad `Show source` selector ambiguous and left mobile Video evidence partially below a fixed, non-scrollable canvas. Focused artifacts now scroll vertically at 390px, use a compact 16:9 Video player, expose all four claim rows, and retain zero horizontal overflow. The refreshed production snapshot suite passes all 27 tests across 1200px desktop, 1024px compact, and 390px mobile, including exact primary-action, source-trail, playback, original-reveal, reduced-motion, contrast, and 200% logical-zoom checks.
- [x] Verify schema, gate, graph, grounding, rendering, and integration tests.
- [x] Verify failure recovery and partial-package behavior.
- [ ] Verify all submitted links, the repeatable recording setup, and the optional inspection instructions in a fresh ChatGPT/Codex in-app browser session.
  - 2026-07-16 clean public-checkout proof: the optional local inspection instructions pass from commit `4cd938d`; the app returned `WorkshopLM Build Week`, an approved Brief, rendered Video, and two Outputs from the verified acceptance root. Port 3000 was occupied in the test environment, so Next selected 3001 and both routes returned HTTP 200. Keep this open for the final public YouTube/Devpost links and one logged-out in-app-browser pass after submission artifacts exist.
  - 2026-07-16 current-main recheck: the same optional path passes from pushed commit `3d7221b` after the latest generated-copy, source-language, and film-capture changes. This confirms those later milestones did not regress clean installation, the complete recorded seam, local rendering, thumbnail generation, packet verification, or the one-command judge route. Keep the gate open only for the final public links and logged-out post-publication inspection.
- [x] Audit every objective requirement against direct evidence before marking the goal complete.

## Current evidence

- [Public GitHub repository](https://github.com/daniel-p-green/OAI-Build-Week-2026)
- [WorkshopLM interface design system](DESIGN.md)
- [Verified Apps in ChatGPT Figma component inventory](research/openai-apps-figma-component-inventory-2026-07-14.md)
- [UI/UX simplification audit](research/ui-ux-simplification-audit-2026-07-14.md)
- [NotebookLM three-column flow audit](research/NOTEBOOKLM-FLOW-AUDIT-2026-07-15.md)
- [Judge-path interaction rebuild](research/judge-path-interaction-rebuild-2026-07-14.md)
- [Experience acceptance criteria](research/ui-experience-acceptance-criteria-2026-07-14.md)
- [Plugin research and adoption map](research/plugins/README.md)
- [Current UI screenshot gallery](outputs/workshoplm-current-ui/README.md)
- [Focused hand-drawn Sketch at desktop width](apps/web/tests/visual/__screenshots__/desktop-sketch-viewer.png)
- [Focused hand-drawn Sketch at mobile width](apps/web/tests/visual/__screenshots__/mobile-sketch-viewer.png)
- [Focused Presentation fit review](artifacts/ui-review/presentation-fit/README.md)
- [Latest-only Output gallery and focused history screenshots](artifacts/ui-review/outputs-latest-only-desktop-2026-07-16.png)
- [Provider-backed Presentation Version 4 review](artifacts/live-review/presentation-v4/contact-sheet.png)
- [Provider-backed Presentation dependency evidence](artifacts/live-review/presentation-v4/evidence.json)
- [Provider-narrated HyperFrames Video Version 4 review](artifacts/live-review/hyperframes-v4/contact-sheet.png)
- [HyperFrames Video Version 4 motion and provenance evidence](artifacts/live-review/hyperframes-v4/evidence.json)
- [Professional image-replacement desktop snapshot](apps/web/tests/visual/__screenshots__/desktop-image-replacement.png)
- [Repeatable fixture walkthrough draft](outputs/demo-recording-draft/manifest.json)
- [Evidence-gated 2:20 demo edit plan](outputs/demo-film-plan/README.md)
- [Sanitized live-provider film evidence](artifacts/live/provider-run.json)
- [Hash-bound Cedar film narration](outputs/demo-film-narration/manifest.json)
- [Current clean authorized-sample editorial cut](outputs/demo-film-sample/workshoplm-demo-sample.mp4)
- [Diagnostic evidence-state rough cut](outputs/demo-film-rough-cut/workshoplm-demo-rough-cut.mp4)
- [Comprehensive WorkshopLM project overview deck](outputs/workshoplm-project-overview.pptx)
- [Traced submission Output-set builder and verifier](apps/worker/src/submission-package.ts)
- [Live-provider authorization and recovery runbook](docs/planning/2026-07-15-live-provider-authorization.md)
- [Dated provider cost envelope](docs/planning/2026-07-15-provider-cost-envelope.md)
- [Realtime voice capture UI](apps/web/app/realtime-capture.tsx)
- [Server-only Realtime client-secret boundary](apps/web/app/api/realtime/realtime-server.ts)
- [Fresh Codex plugin-host evidence](artifacts/spikes/plugin-host-2026-07-15.json)
- [Isolated Codex skill and grounded-read proof](artifacts/spikes/plugin-skill-activation-2026-07-15.json)
- [Judge-facing public claim ledger](submission/CLAIM-LEDGER.md)
- [Product opportunity](research/product-opportunity.md)
- [Workflow map](research/workflow-map.md)
- [NotebookLM live user-flow map](research/notebooklm-user-flow.md)
- [Interface direction](research/interface-direction.md)
- [Engineering direction](research/engineering-direction.md)
- [OpenAI architecture](research/openai-architecture.md)
- [OpenAI API capability roadmap](research/openai-api-capability-roadmap-2026-07-15.md)
- [Unified ChatGPT/Codex plugin architecture](research/unified-plugin-architecture.md)
- [Codex app-server account/session boundary](research/codex-app-server-auth.md)
- [Whiteboard and production research](research/whiteboard-storyboard-production.md)
- [Naming and mission](research/naming-and-mission.md)
- [Hackathon reference](research/hackathon/README.md)
- [Submission checklist](research/hackathon/SUBMISSION-CHECKLIST.md)
- [Execution runbook](PLAN-2026-07-13.md)
- [Foundation and spike implementation plan](docs/superpowers/plans/2026-07-13-foundation-and-integration-spikes.md)
- [Build log](log.md)

## Explicit non-goals

- Education or student workflows.
- A general-purpose Canva replacement.
- A second independent whiteboard engine.
- Every possible connector or export format.
- Autonomous publishing to public channels.
- Hosted multi-user deployment, accounts, and cloud storage for the hackathon demo.
- Video generation that bypasses storyboard approval.
- Claims of perfect image consistency, zero iteration, or production readiness without proof.

## Updating this file

- Update `Last updated`, `Status`, and the relevant checkboxes after meaningful progress.
- Check an item only when the referenced file, command, runtime, test, or external surface proves it.
- Keep future work unchecked even when its design is documented.
- Put detailed historical evidence in `log.md`; keep this file focused on current truth and remaining work.
- If scope changes, update the objective, locked decisions, progress checklist, and non-goals together.
