# WorkshopLM public claim ledger

Last reconciled: 2026-07-17 CT.

Use this ledger before recording, editing the Devpost entry, or publishing screenshots. A claim is eligible only when the named evidence exists, has been inspected, and still matches the recorded product. If a gate is not met, use the bounded wording or omit the claim.

## Claims supported now

| Public claim | Allowed wording | Direct evidence | Boundary |
| --- | --- | --- | --- |
| End-to-end local workflow | “The recorded fixture moves grounded Sources through an editable Map, approved Brief, Style, connected created work, approved Storyboard, and local Video.” | `pnpm demo:e2e`; `outputs/demo-recording-draft/manifest.json`; `.workshoplm/acceptance/generated/submission-output-set-v1/manifest.json` | The no-credential fixture and provider-backed run are separate proof paths. |
| Source grounding | “Factual claims in the recorded Presentation and Infographic link back to exact Source excerpts and locators; local search ranks normalized evidence with SQLite FTS5/BM25.” | `pnpm demo:e2e`; `artifacts/spikes/grounding-fts5-2026-07-15.json`; focused-object `Show source` path | Do not imply every creative element is verified evidence or call local retrieval hosted OpenAI File Search. |
| Editable Map | “The Excalidraw Map supports direct movement, resize, text editing, source inspection, and WorkshopLM Undo.” | `apps/web/tests/visual/completed-ui.spec.ts`; worker graph-history tests; responsive Map screenshots | Do not imply mobile spatial editing; mobile is a review outline. |
| Approval control | “WorkshopLM has exactly two blocking approvals: Brief and Storyboard.” | Recorded acceptance gates; worker gate tests; current UI | Do not describe other review steps as blocking approvals. |
| Brief and Style contracts | “The approved Map becomes a readable Brief; Style preserves palette, type, layout, and negative rules.” | Current Brief and Style views; exported `FRAME-v1.md`, `DESIGN-v1.md`, and token file in the acceptance fixture | Technical filenames belong only in Details or export discussion. |
| Source-traceable created work | “WorkshopLM creates a shareable hand-drawn Sketch, editable Presentation, and one-page Infographic with reopenable versions and retained Source links, alongside a coherent six-panel Image set and editable five-panel Storyboard.” | `pnpm demo:e2e`; production-browser Sketch and version-history proof; submission manifest; rendered HTML artifacts; both `.pptx` files; `artifacts/live/provider-run.json`; `artifacts/live-review/gpt-image-2-contact-sheet.png` | Sketch is a regenerable view of the approved Map, not a second editor. The final founder-derived package remains gated even though the authorized sample run is provider-backed. |
| Created-work history and freshness | “Created work retains real previews, Source coverage, versions, and `Needs update` state.” | Production-route UI test `Outputs preserve recognizable version history and source coverage`; responsive screenshots; five orientation reviews | The test name is historical. The reviews prove screenshot orientation, not uncoached use of an empty Workshop. |
| Professional return visit | “Adding a new meeting expands the Map, marks dependent work `Needs update`, preserves the saved Style, and produces a new Presentation version traced to the new Source after Brief reapproval.” | Worker weekly-return regression; `artifacts/spikes/weekly-return-visit-2026-07-15.json`; inspected browser screenshot | Proven with an isolated local Workshop, not yet with a returning external user. |
| Editable professional Presentation candidate | “WorkshopLM produced a nine-page AI Collective organizer Presentation with varied layouts, concise citations, official supplied branding, and an editable PowerPoint export.” | `outputs/dogfood-ai-collective-chapter-brief/`; `artifacts/spikes/external-deck-dogfood-2026-07-15.json` | Call it a dogfood candidate, not externally approved, until a real professional returns `Ready` or names a required revision. |
| Local video rendering | “The local HyperFrames worker renders only the approved current Storyboard. The inspected provider run uses five OpenAI Cedar clips and emits an H.264/AAC MP4 with per-scene provenance.” | `artifacts/live/provider-run.json`; `artifacts/live-review/hyperframes-video-v2-contact-sheet.png`; Video provenance sidecar; `pnpm demo:e2e` | The sanitized no-credential fixture still uses disclosed deterministic fallback narration. |
| Local-first judge path | “The sanitized fixture runs without an account, private connector, paid API call, or judge-supplied credits.” | `demo:reset`, `demo:e2e`, and submission build/verify commands | Provider-backed creation remains a separate gated path. |
| Unified Codex plugin | “The packaged plugin exposes grounded local search and fetch plus version-gated workflow writes. A fresh Codex desktop task activated `$workshoplm`, fetched exact grounded evidence, and opened the local Workshop doorway; an isolated service run proved the write path through queued Video.” | `artifacts/spikes/plugin-desktop-host-2026-07-15.json`; `artifacts/spikes/plugin-skill-activation-2026-07-15.json`; `artifacts/spikes/plugin-supported-host-2026-07-15.json`; plugin server/service tests and the write-path entry in `log.md` | Codex desktop/CLI is the supported executable host. The separately installed classic ChatGPT app exposed no unified local-plugin surface, so ChatGPT Work support is not claimed; no `/feedback` Session ID is inferred. |
| Codex contribution | “Codex accelerated implementation and verification across the monorepo, local state, plugin, UI, worker, and evidence path.” | Dated commits; `log.md`; package checks and acceptance records | Do not substitute a disposable task ID for the required `/feedback` Session ID. |
| GPT-5.6 build and runtime contribution | “Codex on GPT-5.6 accelerated implementation and verification. In the product, `gpt-5.6-terra` produced the inspected grounded Map after two Sol/Terra/Luna benchmark passes selected it as the default.” | Dated commits and task history; `artifacts/live/provider-run.json`; benchmark artifacts and `log.md` | Keep Codex contribution and API runtime provenance distinguishable. The final founder Source remains open. |
| GPT Image 2 visual batch | “One inspected run generated six 1024×1024 GPT Image 2 panels with a shared Style/Visual DNA reference; all six were accepted after contact-sheet review.” | `artifacts/live/provider-run.json`; `artifacts/live-review/gpt-image-2-contact-sheet.png`; six recorded hashes and request IDs | Do not claim perfect consistency or zero-iteration performance generally. |
| OpenAI Cedar narration | “The grounded Audio Overview is a 33.5-second `gpt-4o-mini-tts` Cedar WAV; the provider-backed product Video uses five Cedar clips; the 2:20 clean editorial review cut uses ten additional hash-bound Cedar clips.” | `artifacts/live-review/audio-overview.json`; exact WAV hash; `artifacts/live/provider-run.json`; `outputs/demo-film-narration/manifest.json`; `outputs/demo-film-sample/manifest.json` | Disclose AI-generated voice. The no-credential fixture replays the exact sanitized Audio Overview bytes, but its local Video still uses fallback narration. The clean review cut is not the final public demo. |
| Realtime grounded conversation | “A live `gpt-realtime-2.1` WebRTC conversation searched and fetched exact Workshop Sources, persisted transcript and tool provenance, handled interruption, and completed one explicitly confirmed Brief approval.” | `artifacts/live-review/realtime-grounded-conversation.json`; `realtime-interruption.json`; `realtime-confirmed-write.json`; corresponding screenshots | Controlled Chrome proved the provider path. The Codex in-app browser microphone permission remained ungranted during automation. |
| Self-build trace | “Every rendered Video carries a build record from the raw transcript and grounded Sources through approved inputs, hashed created work, Build Week commits, and honest provider limitations.” | Version-specific `BUILD-TRACE.html` and `BUILD-TRACE.json`; Video build-trace hashes; submission manifest | The fixture and provider-backed sample must retain their distinct evidence counts and limitations. |
| Current verification floor | “The current build passes checks across 13 packages, 129 worker tests, 32 web tests, 32 production-browser scenarios, the recorded acceptance seam, draft-film truth verification, and the hash-bound clean sample-film verifier.” | Latest `pnpm check`, production-route Playwright, `pnpm demo:e2e`, `pnpm demo:film:verify`, and `pnpm demo:film:verify-sample` entries in `log.md` | Re-run before final publication; counts may change as tests are added. |

## Claims that remain gated

| Claim | Evidence required before use | Until then |
| --- | --- | --- |
| ChatGPT Work support | A future installed Work surface that exposes and invokes the bundled local plugin | Not a current product or submission claim. The verified supported host is Codex desktop/CLI with the local in-app browser. |
| Founder-derived live package | A dated founder recording/transcript imported into the final Workshop, followed by the bounded Terra/Image 2/Cedar run and a verified ready submission manifest | The authorized sample proves provider behavior, not the final founder-derived submission. |
| Original pre-code brainstorm | An authentic dated pre-code artifact | It cannot be backfilled. A new recording must be called a contemporaneous founder brainstorm. |
| Professional-quality validation | A cold professional reviews the external-use Presentation candidate and returns `Ready` or names the first required revision | Say the editable candidate passed deterministic and visual inspection, not external approval. |
| Final self-produced submission | Final Devpost copy, README narrative, Presentation, Infographic, images, Audio Overview, thumbnails, Storyboard, narration, and public Video in one verified connected Workshop, with provider-backed media where claimed | Say Codex built WorkshopLM from the goal file and WorkshopLM produced a traced `partial` recorded-fixture set. Do not say the product built itself or produced the final public submission. |
| Public judge experience | Final under-three-minute public YouTube video, logged-out verification, stable release tag, and working submitted links | Keep all drafts and local recordings labeled as drafts. |

## Final publication gate

Before the Devpost entry or video is final:

1. Re-run `pnpm check`, the production-route UI suite, `pnpm demo:e2e`, `pnpm submission:build`, `pnpm submission:verify`, and `pnpm submission:packet:verify`.
2. Resolve each gated claim above from an inspected artifact, or remove it from the final copy.
3. Confirm the screen labels and narration use `Sources`, `Map`, `Brief`, `Style`, `Created work`, `Presentation`, `Infographic`, `Audio Overview`, `Sketch`, `Storyboard`, `Video`, and `Show source`.
4. Verify the final video against the product state shown; the video and description must not promise behavior the build does not demonstrate.
5. Record the primary Codex `/feedback` Session ID, public YouTube URL, stable release tag, and logged-out link checks in `log.md`.
